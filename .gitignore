node_modules
.DS_Store
dist
dist-ssr
*.local
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# env: never commit real values, but the example must be tracked
.env
.env.*
!.env.example

# local operator config (config.example/ is tracked)
/config/

# turbo
.turbo

# typescript
*.tsbuildinfo

# tests
coverage
playwright-report
test-results

# compose secrets: real values, generated by the quick start
/secrets/
