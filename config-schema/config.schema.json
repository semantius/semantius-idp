{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "server": {
      "type": "object",
      "properties": {
        "baseUrl": {
          "type": "string",
          "description": "Issuer. Scheme + host[:port] + optional path, no trailing slash. Every absolute URL the IdP emits derives from this value only."
        },
        "host": {
          "default": "0.0.0.0",
          "description": "Listen address. Also settable with HOST.",
          "type": "string"
        },
        "port": {
          "description": "Listen port. Also settable with PORT.",
          "type": "integer",
          "minimum": 1,
          "maximum": 65535
        },
        "trustProxy": {
          "description": "Honour X-Forwarded-* from the immediate upstream (true) or from the listed CIDR ranges. Client IP is the rightmost untrusted hop.",
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "array",
              "items": {
                "type": "string",
                "pattern": "^[0-9a-fA-F.:]+\\/\\d{1,3}$"
              }
            }
          ]
        },
        "trustedOrigins": {
          "description": "CSRF origin allow-list. Defaults to [server.baseUrl].",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "allowInsecureHttp": {
          "description": "Permit a non-https baseUrl outside localhost. Development only.",
          "type": "boolean"
        },
        "shutdownTimeoutSeconds": {
          "description": "SIGTERM drain budget.",
          "type": "integer",
          "minimum": 0,
          "maximum": 300
        }
      },
      "required": [
        "baseUrl"
      ],
      "additionalProperties": false
    },
    "secret": {
      "type": "string",
      "description": "≥ 32 random bytes. Fallback env: BETTER_AUTH_SECRET. Must be a placeholder in production."
    },
    "database": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "minLength": 1,
          "description": "Postgres connection string. Fallback env: DATABASE_URL."
        },
        "directUrl": {
          "description": "Connection string for steps that hold a session advisory lock — startup, migrations, the CLI and the cleanup job. Required when `database.url` points at a transaction-mode connection pooler (Neon's `-pooler` endpoint, PgBouncer), where session locks do not hold. Fallback env: DATABASE_URL_ADMIN.",
          "type": "string",
          "minLength": 1
        },
        "schema": {
          "default": "idp",
          "description": "All IdP tables and the drizzle migrations table live here; nothing is created in `public`.",
          "type": "string",
          "pattern": "^[a-z_][a-z0-9_]*$"
        },
        "ssl": {
          "description": "Overrides the connection string's sslmode. Defaults to `require` unless the host is localhost.",
          "type": "string",
          "enum": [
            "disable",
            "require",
            "verify-full"
          ]
        },
        "sslCa": {
          "description": "PEM certificate authority, typically `${file:/run/secrets/ca.pem}`.",
          "type": "string"
        },
        "poolMax": {
          "description": "Maximum pooled connections. One instance is the supported topology (OPS-11), so this is the whole deployment's budget.",
          "type": "integer",
          "minimum": 1,
          "maximum": 200
        },
        "connectTimeoutSeconds": {
          "description": "How long a new connection may take before start-up gives up.",
          "type": "integer",
          "minimum": 1,
          "maximum": 300
        },
        "migrateOnBoot": {
          "description": "Also settable with IDP_MIGRATE_ON_BOOT.",
          "type": "boolean"
        }
      },
      "required": [
        "url"
      ],
      "additionalProperties": false
    },
    "site": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Branding name; also the default TOTP issuer label."
        },
        "adminTitle": {
          "description": "What the administration area calls itself, when that is not `site.name` — a deployment whose users think of it as \"User Manager\" rather than as the identity provider (D61). Branding only: it never reaches the TOTP issuer label, the e-mails or anything a token carries. Defaults to `site.name`.",
          "type": "string",
          "minLength": 1
        },
        "logo": {
          "description": "Path under `branding/` or an absolute URL.",
          "type": "string"
        },
        "favicon": {
          "description": "As `site.logo`. Both `icon.png` and `branding/icon.png` name the same file (D44).",
          "type": "string"
        },
        "supportEmail": {
          "description": "Shown to someone who cannot get in — on the suspended-account page, and in the rejection e-mail.",
          "type": "string",
          "format": "email",
          "pattern": "^(?!\\.)(?!.*\\.\\.)([A-Za-z0-9_'+\\-\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$"
        },
        "termsUrl": {
          "description": "Linked from the sign-up form and the consent screen.",
          "type": "string"
        },
        "privacyUrl": {
          "description": "Linked from the sign-up form and the consent screen.",
          "type": "string"
        },
        "theme": {
          "default": "system",
          "description": "`system` follows the browser. The built-in themes are the only ones; there is no custom CSS hook in v1.",
          "type": "string",
          "enum": [
            "system",
            "light",
            "dark"
          ]
        },
        "defaultLocale": {
          "default": "en-US",
          "description": "Only `en-US` ships in v1 (FR-I18N-1).",
          "type": "string"
        },
        "nameFormat": {
          "default": "first-last",
          "description": "How a display name is composed from the first and last name that were captured (D49). `first-last` gives \"Jane Smith\"; `last-first` gives \"Smith, Jane\". The name itself is never an input field.",
          "type": "string",
          "enum": [
            "first-last",
            "last-first"
          ]
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": false
    },
    "email": {
      "default": {},
      "type": "object",
      "properties": {
        "resend": {
          "default": {},
          "type": "object",
          "properties": {
            "apiKey": {
              "description": "Absent ⇒ degraded mode: every e-mail feature is disabled (FR-MAIL-2).",
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        "from": {
          "description": "Required whenever an API key is configured.",
          "type": "string"
        },
        "replyTo": {
          "description": "Where a reply goes. Absent, replies go to `email.from`, which is usually a mailbox nobody reads.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "signUp": {
      "default": {},
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Governs password and social registration alike (FR-SIGNUP-1).",
          "type": "boolean"
        },
        "requireApproval": {
          "description": "Self-registrations land as `pending` (FR-SIGNUP-2).",
          "type": "boolean"
        },
        "allowedEmailDomains": {
          "description": "Empty = no restriction. Admin-created users always bypass it.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        }
      },
      "additionalProperties": false
    },
    "auth": {
      "default": {},
      "type": "object",
      "properties": {
        "defaultRedirect": {
          "default": "/account",
          "description": "Where a completed sign-in lands when no OAuth continuation and no validated returnTo apply. Relative path, or an absolute URL when the IdP sits beside the product.",
          "type": "string"
        },
        "requireEmailVerification": {
          "description": "Gates password sign-in only. Forced false when e-mail is not configured.",
          "type": "boolean"
        },
        "password": {
          "default": {},
          "type": "object",
          "properties": {
            "minLength": {
              "description": "Length is the only strength rule here. Composition rules push people towards P@ssw0rd1 and are not worth the support load.",
              "type": "integer",
              "minimum": 8,
              "maximum": 128
            },
            "maxLength": {
              "description": "An upper bound exists because the hash is computed over the whole value; it is not a strength policy.",
              "type": "integer",
              "minimum": 12,
              "maximum": 512
            },
            "breachCheck": {
              "description": "Check the password against Have I Been Pwned at sign-up and change.",
              "type": "boolean"
            }
          },
          "additionalProperties": false
        },
        "passwordReset": {
          "default": {},
          "type": "object",
          "properties": {
            "tokenTtlMinutes": {
              "description": "Reset links expire after this; verification links are fixed at 24 h.",
              "type": "integer",
              "minimum": 1,
              "maximum": 1440
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "session": {
      "default": {},
      "type": "object",
      "properties": {
        "expiresIn": {
          "description": "How long a browser session lives without being renewed.",
          "default": "7d",
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "string"
            }
          ]
        },
        "updateAge": {
          "description": "A session older than this is extended on the next request.",
          "default": "1d",
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "string"
            }
          ]
        },
        "cookieCacheMinutes": {
          "description": "Capped at 5 so revocations bite quickly (FR-AUTH-5).",
          "type": "integer",
          "minimum": 0,
          "maximum": 5
        },
        "freshAgeMinutes": {
          "description": "Sensitive actions require a session fresher than this.",
          "type": "integer",
          "minimum": 1,
          "maximum": 1440
        },
        "revokeOAuthTokensOnLogout": {
          "description": "Whether signing out of the IdP also kills the OAuth tokens issued to clients from that session. Off by default: a user closing this tab does not usually mean to sign out of every application they use (FR-AUTH-6).",
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "social": {
      "description": "Keyed by provider id — `google`, `github`, `microsoft`. Each entry needs `clientId` and `clientSecret`; `microsoft` also needs `tenantId` (FR-SOC-5).",
      "default": {},
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {
        "type": "object",
        "properties": {
          "enabled": {
            "type": "boolean"
          },
          "clientId": {
            "type": "string",
            "minLength": 1,
            "description": "From the provider's own console."
          },
          "clientSecret": {
            "type": "string",
            "minLength": 1,
            "description": "A `${env:…}` placeholder in production; never a literal."
          },
          "syncProfile": {
            "description": "Refresh profile fields from the provider on every sign-in (FR-SOC-4).",
            "type": "boolean"
          },
          "allowedEmailDomains": {
            "description": "Restricts this provider to addresses in these domains. Empty = no restriction.",
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "tenantId": {
            "description": "Required for `microsoft`: a tenant GUID or verified domain (FR-SOC-5).",
            "type": "string"
          }
        },
        "required": [
          "clientId",
          "clientSecret"
        ],
        "additionalProperties": {}
      }
    },
    "twoFactor": {
      "default": {},
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether users may enrol at all. Enrolment is per user and always optional (FR-2FA-1); turning this off hides the whole feature.",
          "type": "boolean"
        },
        "issuer": {
          "description": "TOTP issuer label. Defaults to site.name.",
          "type": "string"
        },
        "trustDeviceDays": {
          "description": "How long a device stays trusted after a successful challenge. `0` asks every time and removes the checkbox.",
          "type": "integer",
          "minimum": 0,
          "maximum": 365
        }
      },
      "additionalProperties": false
    },
    "apiKeys": {
      "default": {},
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Off hides the account page and 404s its route (FR-KEY-1).",
          "type": "boolean"
        },
        "defaultExpiresIn": {
          "description": "Pre-filled expiry on the create form.",
          "default": "365d",
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "string"
            }
          ]
        },
        "maxExpiresIn": {
          "description": "The longest expiry a user may choose.",
          "default": "730d",
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "string"
            }
          ]
        },
        "tokenClientId": {
          "default": "idp",
          "description": "`azp` of JWTs exchanged from an API key (FR-KEY-3).",
          "type": "string",
          "minLength": 1
        },
        "tokenTtl": {
          "description": "Lifetime of a JWT exchanged from a key (FR-KEY-3).",
          "default": 3600,
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "string"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "jwt": {
      "type": "object",
      "properties": {
        "algorithm": {
          "default": "ES256",
          "description": "Neon validates ES256 and RS256 only; any other algorithm is rejected at startup.",
          "type": "string",
          "enum": [
            "ES256",
            "RS256"
          ]
        },
        "audience": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "minItems": 1,
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          ],
          "description": "Default RFC 8707 resource, applied whenever a client sends no `resource` parameter. Becomes the JWT `aud`."
        },
        "includeUserData": {
          "description": "Whether access tokens carry the user's name, address and roles at all. Off removes exactly that set (FR-OIDC-7).",
          "type": "boolean"
        },
        "userClaims": {
          "description": "Subset selector over {email, name, given_name, family_name, roles}. Defaults to all of them.",
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "email",
              "name",
              "given_name",
              "family_name",
              "roles"
            ]
          }
        },
        "claims": {
          "description": "Static claims merged into access tokens, e.g. `{ \"role\": \"authenticated\" }` for Neon/PostgREST.",
          "default": {},
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "number"
              },
              {
                "type": "boolean"
              }
            ]
          }
        },
        "claimsInIdToken": {
          "description": "Also merge `jwt.claims` into ID tokens. Off by default: an ID token asserts authentication, and profile data belongs at userinfo.",
          "type": "boolean"
        },
        "rotationInterval": {
          "description": "How often a new signing key is created. The old one keeps verifying for `jwt.gracePeriod` (FR-OIDC-16).",
          "default": "90d",
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "string"
            }
          ]
        },
        "gracePeriod": {
          "description": "Retired keys stay published for this long. Defaults to the longest token lifetime + 1 h.",
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "string"
            }
          ]
        },
        "sessionToken": {
          "default": {},
          "type": "object",
          "properties": {
            "ttl": {
              "description": "Lifetime of a JWT from `GET /api/auth/token`, the first-party session exchange (FR-OIDC-14).",
              "default": 3600,
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string"
                }
              ]
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "oauth": {
      "default": {},
      "type": "object",
      "properties": {
        "accessTokenTtl": {
          "description": "Also the window in which a revoked token still verifies for a stateless resource server (FR-OIDC-12).",
          "default": "15m",
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "string"
            }
          ]
        },
        "idTokenTtl": {
          "default": "1h",
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "string"
            }
          ]
        },
        "codeTtl": {
          "description": "Authorization codes are single-use as well as short-lived.",
          "default": "60s",
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "string"
            }
          ]
        },
        "refreshTokenTtl": {
          "description": "Sliding: every use rotates the token and restarts this clock.",
          "default": "30d",
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "string"
            }
          ]
        },
        "refreshTokenMaxLifetime": {
          "description": "The ceiling the sliding window cannot pass. After this the user signs in again.",
          "default": "90d",
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "string"
            }
          ]
        },
        "scopes": {
          "description": "Every scope any client may request. A client's own `scopes` must be a subset.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "resources": {
          "description": "Extra RFC 8707 resources beyond `jwt.audience` and the per-client ones, each optionally with its own allowed scopes and token lifetime.",
          "type": "array",
          "items": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "object",
                "properties": {
                  "identifier": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  },
                  "allowedScopes": {
                    "type": "array",
                    "items": {
                      "type": "string",
                      "minLength": 1
                    }
                  },
                  "accessTokenTtl": {
                    "anyOf": [
                      {
                        "type": "number"
                      },
                      {
                        "type": "string"
                      }
                    ]
                  }
                },
                "required": [
                  "identifier"
                ],
                "additionalProperties": false
              }
            ]
          }
        },
        "reconcile": {
          "default": {},
          "type": "object",
          "properties": {
            "prune": {
              "description": "Delete rows for clients no longer in the file instead of disabling them (FR-OIDC-2).",
              "type": "boolean"
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "admin": {
      "default": {},
      "type": "object",
      "properties": {
        "adminRoles": {
          "description": "Holding any of these opens `/admin` and the admin API. Every name must exist in the role catalog.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "allowImpersonation": {
          "description": "Lets an administrator act as another user, ≤ 1 h, never against another administrator, every action audited (FR-ADMIN-5).",
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "rateLimit": {
      "default": {},
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Turning this off removes the SEC-2 limits on sign-in, reset, 2FA and the token endpoint. For tests.",
          "type": "boolean"
        },
        "storage": {
          "default": "database",
          "description": "`database` survives a restart, which is the point of a limit. `memory` is for a single process nobody restarts to get past it.",
          "type": "string",
          "enum": [
            "database",
            "memory"
          ]
        }
      },
      "additionalProperties": false
    },
    "logging": {
      "default": {},
      "type": "object",
      "properties": {
        "level": {
          "default": "info",
          "description": "Also settable with LOG_LEVEL.",
          "type": "string",
          "enum": [
            "trace",
            "debug",
            "info",
            "warn",
            "error"
          ]
        },
        "format": {
          "default": "json",
          "description": "`json` for anything that collects logs; `pretty` for a terminal. Also settable with LOG_FORMAT.",
          "type": "string",
          "enum": [
            "json",
            "pretty"
          ]
        }
      },
      "additionalProperties": false
    },
    "cleanup": {
      "default": {},
      "type": "object",
      "properties": {
        "intervalMinutes": {
          "description": "How often the retention job runs: expired sessions, spent verification rows, dead tokens, stale rate-limit rows and retired keys (OPS-8, DM-5).",
          "type": "integer",
          "minimum": 1,
          "maximum": 1440
        }
      },
      "additionalProperties": false
    },
    "audit": {
      "default": {},
      "type": "object",
      "properties": {
        "retentionDays": {
          "description": "How long audit rows are kept. They are the answer to what happened, so a short window is a decision, not a saving.",
          "type": "integer",
          "minimum": 1,
          "maximum": 3650
        }
      },
      "additionalProperties": false
    }
  },
  "required": [
    "server",
    "secret",
    "database",
    "site",
    "jwt"
  ],
  "additionalProperties": false,
  "title": "semantius-idp config.jsonc"
}
