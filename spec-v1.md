# semantius-idp — Specification v1

**Status:** signed off (2026-08-23) · **Date:** 2026-08-23 · **Supersedes:** `spec-v0.md` (kept as history)
**Scope of this document:** complete, numbered requirements for v1. No implementation plan, no code. The implementation plan is derived from this document after sign-off.

Sources: `spec-v0.md`, owner decisions D1–D26 and Q1–Q16 (§12), verified facts V1–V8 (§12.3), independent plan audit (applied; audit-set defaults are marked *(audit default)*). Implementation risks that must be verified in an early spike are listed in §13 — requirements depending on them reference the risk id.

---

## 1. Purpose, goals, non-goals

### 1.1 Purpose

A **lightweight, self-hosted identity provider (IdP)** delivered as a single Docker container. It provides user management and authentication for internal applications and issues **standard JWT access tokens** that are validated via **JWKS** by resource servers — explicitly including **Neon** (RLS / Data API) and Supabase-style PostgREST consumers.

### 1.2 Goals

- G1 — Single-tenant OIDC/OAuth 2.1 provider: login, sign-up with approval, password reset, social sign-in, 2FA, per-user API keys. (Machine-to-machine / `client_credentials` is **out of scope for v1**, D26.)
- G2 — **Configured by files + environment**: `config.json`, `oauth_clients.json`, `roles.json` in a config folder are the source of truth; secrets arrive via environment variables or secret files. A container is fully configured without touching the database.
- G3 — **JWTs that Neon accepts out of the box**: ES256, `kid` in the header, JWKS + discovery served at the issuer, audience configurable.
- G4 — Lean and self-contained: one container + Postgres; the only optional external services are Resend (e-mail) and the enabled social providers.
- G5 — Operable: health endpoints, structured logs, audit trail, automated migrations, documented runbooks, automated tests.
- G6 — Runs standalone (local/dev) or behind a Caddy reverse proxy, **including under a URL sub-path** (e.g. `https://apps.example.com/idp`).

### 1.3 Non-goals (v1)

Multi-tenancy/organizations · billing · **machine-to-machine tokens** (`client_credentials` grant and `service` clients — D26, v1.1) · **account linking** (deliberately absent, see FR-SOC-2) · SAML · SCIM/LDAP/external user stores · passkeys · magic links / e-mail OTP login · user-supplied template/theme overrides (built-in light/dark stays) · pairwise subject identifiers · generic upstream OIDC providers (`genericOAuth` — v1.1) · SMTP transport (v1.1; v1 is Resend-only) · configuration hot reload · multi-replica operation (replica-*safe*, not replica-*supported*) · metrics/OpenTelemetry · locales beyond en-US (structure prepared) · load/performance testing beyond the footprint targets in OPS-13.

---

## 2. Glossary & actors

| Term | Meaning here |
|---|---|
| **IdP** | This product: the auth server + its UI in one container. |
| **End user** | A person with a `user` row; signs in at the IdP. |
| **Admin** | An end user holding a role listed in `admin.adminRoles`. Only admins reach `/admin/*` and the admin API. |
| **Client / RP** | An application registered in `oauth_clients.json` that sends users through the OIDC authorization-code flow. |
| **First-party app** | A client flagged `firstParty: true` running on the **same host** as the IdP (it shares the host-only session cookie; the sub-path deployment exists precisely for this). |
| **Resource server / verifier** | Anything that validates the IdP's JWTs via the JWKS: Neon (proxy + `pg_session_jwt`), Supabase-style PostgREST, internal APIs using `jose`. Neon is a **verifier, not a client**. |
| **Issuer** | The exact value of `server.baseUrl` — scheme + host[:port] + optional path, no trailing slash. |
| **Audience / resource** | RFC 8707 resource identifier; becomes the JWT `aud` claim. |
| **Operator** | Whoever deploys/configures the container. |
| **Catalog role** | A role defined in `roles.json`; stored per user in the Better-Auth column `user.role` (comma-separated), emitted as the `roles` array claim. |

---

## 3. Architecture

One deployable unit: a **TanStack Start** application (default SSR; server routes host all protocol endpoints) executed by **Bun**, embedding **Better Auth** with the plugins `admin`, `jwt`, `oauth-provider`, `api-key`, `two-factor` (plus a small local plugin contributing the `audit_log` schema and custom endpoints). State lives in **Postgres** (schema `idp` by default). Configuration is read once at startup from the config folder + environment. A **Caddy** reverse proxy (reference config shipped) terminates TLS in production; standalone HTTP is for local/dev only.

```
Browser ──► Caddy ──► IdP container (TanStack Start + Better Auth, Bun)
                         │  /login /account /admin …          (SSR pages)
                         │  /api/auth/* /oauth2/* /.well-known/* /healthz  (server routes)
                         ▼
                      Postgres (schema: idp)

Neon / APIs ──(fetch JWKS over public https)──► {baseUrl}/api/auth/jwks
Client apps ──(OIDC code+PKCE / session JWT / x-api-key)──────────────────────► IdP
```

Frontend stack note: TanStack Start with its **default SSR** (server-side route loaders gate `/admin/*` at first paint; the login page renders without a JS flash). **TanStack Query is not a v1 dependency** — Better Auth's React client plus router loaders/server functions with `router.invalidate()` cover all data needs; Query may be added later without spec impact. UI components: shadcn on `@base-ui/react` from the workspace `packages/ui`.

---

## 4. Technology & versions

| Component | Requirement |
|---|---|
| Better Auth | **Latest stable at implementation start** (1.7.1 at spec time) — exact version pinned and recorded in the decision log; plugins: core, `admin`, `jwt`, `oauth-provider`, `api-key`, `two-factor`. The v0 sample schema is a **naming reference only**; the authoritative schema is generated from the installed Better Auth's own `getAuthTables()` for the enabled plugins (§7, DM-1, D29). |
| Runtime | **Bun**, pinned (`.bun-version` + `engines.bun`). Node is used only by CI tooling; the scaffold's `engines.node >= 20` is amended accordingly. |
| Framework | TanStack Start (default SSR) + TanStack Router; Vite; React 19. |
| UI | shadcn + `@base-ui/react`, Tailwind 4, from `packages/ui`. |
| ORM / DB | Drizzle + postgres driver; Postgres ≥ 16 (compose ships 16/17). |
| Package manager | pnpm, committed lockfile, `--frozen-lockfile` in CI/image. |
| Pinning policy | v0's "latest" = latest stable **at implementation start**, then **pinned exactly** — all dependencies, including every `@tanstack/*` currently at `latest` in the scaffold. Upgrades are explicit changelog entries. |

---

## 5. Functional requirements

Format: each requirement has an id, a normative statement, and acceptance criteria (**AC**). "Config:" names the keys from §6 that govern it.

### 5.1 Authentication — password (FR-AUTH)

**FR-AUTH-1 — Password sign-in.** Users sign in with e-mail + password at `/login`. Passwords: min 10 (configurable, `auth.password.minLength`) / max 128 characters, no composition rules; optional breached-password check behind `auth.password.breachCheck` (default false). E-mails are trimmed and lower-cased everywhere (sign-up, sign-in, social, admin). A completed sign-in resolves its destination in this order (D28): a pending OAuth authorization continuation (FR-OIDC-9) · a `returnTo` query parameter that validates as a same-origin relative path (SEC-3, unchanged) · `auth.defaultRedirect` · `/account`. The same resolver governs **password-change completion**, because FR-AUTH-4 interposes `/change-password` before that destination and an absolute `auth.defaultRedirect` cannot round-trip through a `returnTo` parameter that only accepts relative paths — so the forced-change handler re-resolves at the end rather than carrying the value through the query. Sign-up, password reset and verification keep their own endings.
*AC:* valid credentials create a session; 9-character password rejected at sign-up/change; sign-in failure message is identical for wrong password and unknown e-mail.
*AC (D28):* with `auth.defaultRedirect` unset, sign-in lands on `/account`; with it set to an absolute URL, both a plain sign-in and a forced password change land there; a `returnTo` of `https://evil.example` is still ignored; a bare hostname is rejected at configuration load.

**FR-AUTH-2 — E-mail verification.** `auth.requireEmailVerification` (default **true**; forced false when e-mail is not configured, FR-MAIL-2) gates **password sign-in only**: unverified password accounts cannot sign in and are offered a resend. Social accounts are never gated by it — their `emailVerified` comes from the provider claim (FR-SOC-4); unverified users carry a badge in the admin UI.
*AC:* with verification on, a fresh password sign-up cannot sign in before clicking the e-mailed link; the link is single-use and expires (24 h); resend is rate-limited.

**FR-AUTH-3 — Password reset.** `/forgot-password` sends a reset link when e-mail is configured (FR-MAIL-1); response is identical whether or not the account exists. Reset tokens are single-use, expire after 1 h, and are invalidated by any password change. Completing a reset (or any password change) revokes all other sessions **and all OAuth tokens** of the user (FR-OIDC-12). A notification e-mail is sent on password change.
*AC:* token reuse fails; after reset, an existing refresh token is rejected; the response for an unknown e-mail is byte-identical (modulo timing) to a known one.

**FR-AUTH-4 — Forced password change.** Users flagged `mustChangePassword` (bootstrap admin, admin-set temporary passwords) are interposed with a change-password step before anything else completes (including OAuth continuations, FR-OIDC-9).
*AC:* a `mustChangePassword` user completing `/oauth2/authorize` is routed through the change form before the client redirect.

**FR-AUTH-5 — Sessions & cookies.** Sessions last 7 d sliding (`session.expiresIn`), refreshed when older than 1 d (`session.updateAge`); cookie cache ≤ 5 min so revocations bite quickly. Cookies: `HttpOnly`, `Secure` whenever `server.baseUrl` is https (independent of the internal scheme), `SameSite=Lax`, **host-only** (no `Domain`), `__Secure-` prefix when secure, `Path` scoped to the `baseUrl` path. Every mutation requires a session, read **authoritatively** — past the cookie cache — so a revocation, a suspension or a withdrawn approval bites on the next write rather than whenever the cache happens to expire. There is no *freshness* requirement and no step-up re-authentication (**D81**).
*AC:* cookie attributes asserted in tests; a session of any age creates an API key without a prompt; a POST with no session at all lands on `/login?notice=signin_required` carrying its `returnTo`.

**FR-AUTH-6 — Logout.** `POST {baseUrl}/api/auth/sign-out` deletes the session and clears the cookie; the `/logout` page calls it and redirects to `/login` or a **same-origin** `returnTo`. `session.revokeOAuthTokensOnLogout` (default false) additionally revokes the session's OAuth refresh tokens. OIDC RP-initiated logout is FR-OIDC-11.
*AC:* after sign-out the cookie is expired and `/account` redirects to `/login`.

**FR-AUTH-7 — Mass-assignment protection.** `role`, `banned`, `banReason`, `banExpires`, `emailVerified`, `status`, `approvedAt`, `approvedBy`, `mustChangePassword` are declared `input: false` — settable only through admin/internal paths.
*AC:* a sign-up body containing `{"role": "admin", "status": "active"}` creates a default-role pending user; the fields are ignored or rejected.

### 5.2 Sign-up & approval (FR-SIGNUP)

**FR-SIGNUP-1 — Global sign-up switch.** `signUp.enabled` (default **false**) governs password *and* social registration. When off: `/signup` returns 404 and is unlinked, `emailAndPassword.disableSignUp` is set, and every enabled social provider gets `disableImplicitSignUp` — social sign-in then succeeds **only** for already-registered `(providerId, accountId)` identities (FR-SOC-3).
*AC:* with sign-up off, a password registration attempt gets a 4xx; a Google sign-in by an unknown Google account is refused with a neutral message.

**FR-SIGNUP-2 — Approval workflow.** `signUp.requireApproval` (default **true**) applies to all self-registration paths. Users carry `status ∈ {pending, active, rejected}` plus `approvedAt`, `approvedBy`. Order: sign-up → e-mail verification (when required) → approval. A non-`active` user obtains **no session, no authorization code, no access/refresh token and no API-key authentication on any path** — password, social, `/oauth2/authorize`, `refresh_token` grant, `x-api-key`, session-JWT endpoint (enforced in the session-creation hook and re-checked in the refresh and API-key paths). Pending users see `/pending-approval`. Admin-created and bootstrap users are `active` immediately. Rejected users keep their row (e-mail stays reserved; sign-in shows a neutral refusal); deleting the user afterwards allows a fresh sign-up. Approval/rejection triggers an e-mail to the user, and new pending sign-ups trigger an e-mail to all admins — only when e-mail is configured (FR-MAIL-1).
*AC:* pending user: password login refused, social login refused, refresh grant refused, API key refused; after admin approval the same credentials work; the sign-up→approve→login path is an integration test.

**FR-SIGNUP-3 — Domain restriction.** `signUp.allowedEmailDomains[]` (empty = no restriction) rejects self-registrations whose e-mail domain is not listed; applies to password and social sign-up. Admin-created users bypass it.
*AC:* with `["example.com"]`, `user@gmail.com` cannot self-register.

**FR-SIGNUP-4 — Configuration sanity warnings.** Startup warns (does not fail) on `signUp.enabled && !signUp.requireApproval && e-mail off` ("unverified open registration"). A social provider enabled while `signUp.enabled = false` is a supported configuration and produces **no** warning (D26 → D25: the combination is the normal "invite-only, social sign-in for existing identities" deployment).
*AC:* the "unverified open registration" warning is asserted in config-loader tests; a config with a social provider enabled and `signUp.enabled = false` produces no warning.

**FR-SIGNUP-5 — Name capture.** Sign-up, admin create **and** admin edit collect optional `firstName`/`lastName` — never a free-text `name`. `name` is **derived** from the two parts in `site.nameFormat` order (`first-last` → `"Jane Smith"`, `last-first` → `"Smith, Jane"`), recomputed on every write, and falls back to the e-mail address when both parts are empty (**D49**). Social providers map `given_name`/`family_name` into `firstName`/`lastName` and the name is re-derived from them; a provider that ships only `name` (GitHub) keeps the one it sent, because splitting it would be guessing.

### 5.3 Social sign-in (FR-SOC)

**FR-SOC-1 — Config-driven providers.** Social providers are enabled solely via `config.json → social.<providerId>` for any Better Auth built-in provider id (`google`, `github`, `microsoft` (Entra), …): `{ enabled, clientId, clientSecret (placeholder), …provider options }`. Callback URL per provider is `{baseUrl}/api/auth/callback/<providerId>` and is documented per provider.
*AC:* a provider absent from config does not render a button and its callback route is inert.

**FR-SOC-2 — No account linking, identity by provider subject.** Account linking is **disabled entirely** (no option). A social identity maps to exactly one user via `(providerId, accountId)` where `accountId` is the provider's **stable subject** — for Entra the `oid` claim (verified in Better Auth 1.7.1), never e-mail/UPN. E-mail is profile data only. Consequences (all stated in the README): an existing password user can never attach a social identity; a social sign-in whose e-mail equals another user's e-mail is **rejected** with "sign in with your original method" (e-mail stays unique); a provider-side e-mail change can never map to a different user.
*AC:* password user `a@x.com` + Google account with e-mail `a@x.com` → refusal, no link, no new user; Entra user whose mail changes → same user record.

**FR-SOC-3 — Sign-up interaction.** With sign-up enabled, an unknown `(providerId, accountId)` creates a user (subject to FR-SIGNUP-2/3); with sign-up disabled it is refused (FR-SIGNUP-1).

**FR-SOC-4 — Profile sync.** `social.<p>.syncProfile` (default true) updates `email`, `name`, `image` (and `firstName`/`lastName` when mapped) from the provider on **every** sign-in of an existing account; `emailVerified` follows the provider's claim. If the incoming e-mail collides with **another** user's e-mail, the **sign-in is blocked** (D24): no session is created, the user sees the same neutral "sign in with your original method" refusal as FR-SOC-2, the profile is left untouched, and audit event `social.profile_conflict` is written and flagged in the admin user detail. Rationale: proceeding would leave the account's e-mail permanently diverged from the provider's, and a silent skip hides an identity collision that an admin must resolve. (Risk R7.)
*AC:* user A has e-mail `a@x.com`; user B signs in with a Google identity whose provider e-mail has changed to `a@x.com` → sign-in refused with the neutral message, B's row unchanged, one `social.profile_conflict` audit row.

**FR-SOC-5 — Entra tenant lock.** `social.microsoft.tenantId` is **required** — a tenant GUID or verified tenant domain; `common`, `organizations`, `consumers` are rejected by validation. `social.<p>.allowedEmailDomains` may restrict further.
*AC:* config with `tenantId: "common"` fails validation.

### 5.4 Two-factor authentication (FR-2FA)

**FR-2FA-1 — TOTP, optional per user.** Better Auth `twoFactor` plugin: TOTP + backup codes. Enrolment from the account page (QR + verification; backup codes shown once); disabling requires password re-entry; login gains a 2FA step with "trust this device" for `twoFactor.trustDeviceDays` (default 30). 2FA is enforced at IdP login, so **all OIDC flows inherit it**; API keys bypass it by design (documented). TOTP issuer label = `twoFactor.issuer` (default `site.name`, never `site.adminTitle` — **D61**).
*AC:* enrolled user: password alone insufficient; backup code works once; OAuth authorize routes through the 2FA step.

**FR-2FA-2 — Admin reset.** Admins can reset a user's 2FA (custom action: delete the `two_factor` rows, clear `twoFactorEnabled`, revoke sessions, audit-log).

### 5.5 Account self-service (FR-ACCT)

**FR-ACCT-1 — Account page.** `/account` provides: profile (firstName, lastName, image; the display name is shown **read-only** and re-derived on save — **D49**), change password (current password required; revokes other sessions + OAuth tokens), change e-mail (with verification of the new address; hidden when e-mail is off — admins edit directly), 2FA management, API keys (FR-KEY), active sessions with revoke, granted consents with revoke. **No self-deletion** (admin only). The area wears the sidebar shell (**D82**), and its user menu carries a link to `/admin` for a user holding an admin role, resolved server-side and never from `UiContext`, together with sign-out — which goes through the `GET /logout` confirmation page.

**FR-ACCT-2 — Public UI inventory.** `/login` (password + enabled social buttons + OAuth continuation), `/signup` (404 when disabled), `/forgot-password`, `/reset-password` (valid/expired/invalid states, **decided before the form renders**, naming the account the link is for; `welcome=1` makes it the invitation variant — **D65**), `/verify-email` (result + resend), 2FA challenge, `/consent`, `/pending-approval`, `/banned` (reason/expiry when set), forced-change-password, `/logout`, end-session confirmation, `/account/*` — where changing a password is a **dialog on `/account/security`**, sharing its fields and its rules with the standalone page (**D62**) — error pages (404, 403, 500, OAuth error, 429 with retry-after). All strings from the en-US catalog (FR-I18N-1); WCAG 2.1 AA, labelled inputs with `autocomplete` (`username`, `current-password`, `new-password`), password visibility toggle, inline policy hint, mobile-first; no "remember me" (session length is config). **Forms check what the browser can check before posting, and nothing the user typed is lost when the server refuses** (**D62**): a rule the client can evaluate — the two passwords match, a redirect URI is absolute — is reported inline against its field without a round trip, the server's identical check stays authoritative, and a refusal that does reach the server comes back with the submitted values restored (passwords excepted, deliberately). **In `/account/*`, a success confirmation is a transient toast that strips its own `?notice=` from the address bar; an error stays inline beside the form that produced it** (**D71**). **A confirmation about one account names it**, on a second line under the sentence, and **it is dismissed on wall-clock time** — a window left unfocused cannot pin one to the screen (**D78**). The public pages keep their inline notices, where the message *is* the page (`/logout`, `/forgot-password`, `/verify-email`, `/signup`, `/setup`).

### 5.6 Per-user API keys (FR-KEY)

**FR-KEY-1 — Keys.** Better Auth `apiKey` plugin, gated by `apiKeys.enabled` (default true). Users create keys in `/account` (name + expiry ≤ `apiKeys.maxExpiresIn`, default expiry `apiKeys.defaultExpiresIn` = 365 d, max 730 d); admins can create/list/revoke keys for any user. Keys are hashed at rest, shown once, prefixed for recognisability, revocable, rate-limited per key.
*AC:* a listed key never shows its secret again; a revoked or expired key stops authenticating.

**FR-KEY-2 — Authentication semantics.** A request with `x-api-key` authenticates **as the owning user** (same roles/claims). Keys are rejected when the user is not `active` or is banned (FR-SIGNUP-2, FR-ADMIN-4); they bypass 2FA (documented); they confer admin-API access only when the owner holds an admin role. Creation/revocation/use-failures are audit-logged.

**FR-KEY-3 — JWT exchange.** `GET {baseUrl}/api/auth/token` with `x-api-key` returns a JWT built by the **same claims builder** as OAuth access tokens (FR-OIDC-7): `azp = apiKeys.tokenClientId` (default `"idp"`), random `sid`, `scope = "openid profile email"`, `exp = iat + apiKeys.tokenTtl` (default 3600 s), `aud = jwt.audience`. The key path is recognised by a process-local marker the api-key gate stamps on the session, not by the session's token type (**D41**).
*AC:* the exchanged JWT validates against the JWKS and passes the Neon constraints test (TST-4).

### 5.7 Roles (FR-ROLE)

**FR-ROLE-1 — Catalog in `roles.json`.** Entries `{ name, description, default? }`. Absent file ⇒ built-in catalog `[{ name: "admin" }, { name: "user", default: true }]`. Validation: names unique, `^[a-z0-9_-]{1,64}$` (no commas); **exactly one** `default: true` — it feeds the admin plugin's `defaultRole` and is assigned at self-registration; every entry of `admin.adminRoles` (default `["admin"]`) must exist in the catalog. Roles are **labels for downstream apps**: the IdP evaluates no permissions from them.

**FR-ROLE-2 — Assignment & claims.** A user may hold several catalog roles, stored comma-separated in the Better-Auth-fixed column `user.role` and emitted as the **`roles: string[]`** claim (FR-OIDC-7). **Naming rule:** the singular `role` claim is *never* derived from `user.role`; it exists only as a static value from `jwt.claims` (FR-OIDC-8). Role values no longer in the catalog are dropped from claims, warned at boot, and flagged in the admin UI. Admins assign roles from the catalog; changing definitions = edit `roles.json` + restart.
*AC:* user with `user.role = "admin,billing"` gets `roles: ["admin","billing"]`; an unknown stored role is excluded from the claim and visible as a warning in `/admin/users/:id`.

**FR-ROLE-3 — Admin gating.** Only users holding a role in `admin.adminRoles` can access `/admin/*` (server-side in route loaders **and** at the API), everything else returns 403.

### 5.8 Admin (FR-ADMIN)

**FR-ADMIN-1 — First-run setup (**D52**, supersedes the env bootstrap).** There is no bootstrap account and no credential in configuration. **While the `user` table is empty**, `/` and `/login` redirect to `/setup`, which collects e-mail, first name, last name (all three required, trimmed) and a password **entered twice** (**D54**) and creates the first user: `active`, e-mail verified, the **first role of `admin.adminRoles` plus the catalog's default role, deduplicated when they coincide** (**D69**), **no** forced password change (the person choosing the password is the person using it). The creation re-checks that the table is still empty **inside the `bootstrapAdmin` advisory lock on the direct connection**, so two concurrent submissions produce exactly one account and the loser is told the deployment is already set up. The POST is rate-limited by the table-backed limiter (SEC-2, D37). `signUp.enabled`, `signUp.requireApproval` and `signUp.allowedEmailDomains` do not apply: this is configuring the deployment, not registering. The gate is "the table is empty", **not** "no admin exists", so the page closes for good once any user exists — a deployment that lost its last administrator must not be able to mint one from an unauthenticated page. Start-up logs `no users yet` with the `/setup` URL instead of creating anything. Automatic promotion of the first sign-up remains forbidden. Lockout recovery: a second administrator, the password-reset e-mail, or the single documented SQL promotion in `docs/runbooks.md`.
*AC:* two concurrent submissions create exactly one admin and the loser is told the deployment is already set up; the page is gone on the next visit. *AC (D54):* a blank or whitespace-only first or last name is rejected; a password that does not match its repeat is rejected; the length rule is checked before the repeat, so two short passwords report the length. (The old criterion here — "removing the env vars later does not demote anyone" — went with the env bootstrap in **D52**.)

**FR-ADMIN-2 — Admin UI.** `/admin` dashboard (pending count, user/session totals) · `/admin/users` (search by e-mail/name; filters: role, status, banned, verified; sort; page sizes 25/50/100) · user detail (profile, roles, status, linked identities, sessions, API keys, audit excerpt) · create user, **as a dialog on `/admin/users`** (**D64**) (pre-approved + verified; the default role arrives ticked, which is what an untouched form has always produced; set-password link (marked `welcome=1`, so the page it opens says an administrator made the account) e-mailed when e-mail is on, otherwise shown **one-time** in a dialog on the same page, labelled with the address it is for — through the server-side one-shot stash, never in a query string (**D51** review); or admin sets a temporary password + `mustChangePassword`) · **edit profile** (firstName, lastName, e-mail, verified flag) as an `edit-profile` admin action, with the display name derived (**D49**) · **set roles** as one checkbox per catalog role, on create and on edit (the field repeats and the handler joins) · approve/reject · ban/unban (reason, optional expiry) · set password / send reset · revoke sessions · delete (confirmation; cascades tokens/keys) · 2FA reset (FR-2FA-2) · `/admin/users/:id`'s actions are **links that open dialogs**, each carrying the form it always had, and the destructive ones (delete, ban) confirm inside that dialog — the convention across the admin area: an action is a dialog on the page that lists what it acts on, never a route of its own (**D64**) · `/admin/clients` is a table with **create / edit / rotate secret / disable / remove for database-registered clients** (**D50**, **D72**) — the four per-row actions live behind **one `⋯` menu in a column of their own**, named for the application it acts on (**D80**), the edit dialog carries every writable field prefilled, the client id shown and fixed and file-managed rows read-only with a "managed by oauth_clients.json" badge and no menu at all; `/admin/roles` stays read-only with its "managed by roles.json" notice, last-reconcile timestamp and warnings (the roles-versus-database drift start-up detects — a role held by a user but absent from the catalog, and therefore dropped from their claims — **not** the configuration-load warnings, which `/admin` and `/admin/system` already carry) · `/admin/audit` (filterable; the actor and target columns resolve user ids to display names in one batched query, render `targetType` beside the target, and carry the full ids in `title`) · `/admin/system` (version, masked effective config, e-mail transport status, signing key alg/kid + rotate action, migration state, last reconcile result, and the **discovery / well-known URLs** this deployment answers on, absolute, including **both** origin-root forms under a sub-path — **D55**). The impersonation control is rendered only when `admin.allowImpersonation` is on (round 2, finding 9). The area is titled `site.adminTitle`, falling back to `site.name` — in the sidebar's brand block and in the document title, and nowhere else (**D61**). **The area wears the sidebar shell** (**D82**): a collapsible left sidebar carrying the navigation and, in its footer, the administrator's own identity, the way through to `/account` and sign-out; the page content is full width, and the header row carries the toggle and the area's `<h1>`. **A refusal is reported honestly**: the SEC-7 collapse into "that e-mail address and password combination is not correct" belongs to the public pages and is never shown behind `/admin/*`, where the caller is an authenticated administrator and the form may contain no credential at all — a duplicate address is named as one, and anything unrecognised is a failed request (**D70**). **Creating a user never leaves an account without a way in**: once the account exists, a failure to mint or send its set-password link lands on the list with a notice naming the two recoveries on the account's own page, never on an error page (**D70**). **A success confirmation is a transient toast**, shown once and stripped from the URL that carried it; refusals and the one-time secret dialogs are unchanged (**D71**). **It names the account it is about** — carried across a redirect as a one-shot handle, never as an address in the query string — and **it goes on wall-clock time**, so an unfocused window cannot leave it standing (**D78**).

**FR-ADMIN-3 — Invariants.** Admins cannot change their own roles, ban or delete themselves; the **last admin** cannot be demoted, banned, deleted or set non-active; only admins can grant admin roles; all enforced server-side.

**FR-ADMIN-4 — Ban.** Better Auth OOTB ban: `banned`, `banReason`, `banExpires`; banning revokes sessions **and** OAuth tokens and blocks API keys (FR-OIDC-12); banned users see the reason/expiry at login. Ban fields never appear in JWTs.

**FR-ADMIN-5 — Impersonation (optional).** Behind `admin.allowImpersonation` (default **off**). When on: ≤ 1 h, never against users holding an admin role, session carries `impersonatedBy`, UI shows a persistent banner — rendered once by the shared `SidebarLayout`, above the sidebar row, which is what makes "every page" true by construction (**D82**) — everything audit-logged. During impersonation: no password/e-mail/2FA/API-key changes, no `/oauth2/authorize`, no session-JWT issuance, no admin API.

**FR-ADMIN-6 — Admin HTTP API.** The Better Auth admin endpoints plus the custom approval endpoints form the documented, role-gated management API (this is v0's "pluggable user management"). Callable with an admin session or the `x-api-key` of an admin user.

### 5.9 OAuth 2.1 / OIDC provider (FR-OIDC)

**FR-OIDC-1 — Grants & response types.** Supported: `authorization_code` with **PKCE S256 only** (`plain` rejected; PKCE mandatory for public clients, default-on for confidential) and `refresh_token` — **these two only** (D26). `response_types_supported = ["code"]`. `client_credentials`, implicit, hybrid, ROPC and device-code are disabled, rejected with `unsupported_grant_type`, and absent from `grant_types_supported` in discovery. Authorization codes: single-use, TTL 60 s, bound to client + redirect URI + code challenge; replay of a consumed code revokes the tokens issued from it.

**FR-OIDC-2 — Clients from `oauth_clients.json`.** The file is the **source of truth**; at startup it is validated and reconciled into `oauth_client` (+ resource links) — the operative store Better Auth reads at runtime: insert missing, update changed (secret re-hash on change, `createdAt` preserved), clients absent from the file ⇒ `disabled = true` + revoke their tokens/consents (`oauth.reconcile.prune: true` deletes rows instead, default false). All file ids are registered in `cachedTrustedClients`; dynamic client registration (`/oauth2/register`) stays off; Better Auth's own client CRUD endpoints are denied for every caller (404/403). Reconciliation is transactional under an advisory lock; its diff is audit-logged; validation failure aborts startup. Config-synced rows use `user_id = NULL`, **and the orphan sweep is scoped to exactly those rows** — which is what lets D50's admin-registered clients (`user_id` = creating admin) coexist untouched across every restart. A client id present in both the file and the database becomes file-managed at the next reconcile.
*AC:* editing a redirect URI in the file + restart changes behaviour; an admin calling the create-client endpoint gets 403/404; removing a client (prune off) leaves a disabled row and its refresh token stops working.

**FR-OIDC-3 — Client schema.** Per entry: `clientId` (required, unique), `name`, `type: "web" | "spa" | "native"` (D26 removed `service`; mapping: `spa`/`native` ⇒ public — no secret, `tokenEndpointAuthMethod: "none"`, PKCE required; `web` ⇒ confidential — `clientSecret` placeholder ≥ 32 chars, `client_secret_basic` (default) or `client_secret_post`), `firstParty` (FR-OIDC-14), `redirectUris` (absolute, exact-match, no wildcards/fragments, https except loopback `http://127.0.0.1` / `http://localhost`; private-use schemes allowed for `native`), `postLogoutRedirectUris`, `scopes` (⊆ `oauth.scopes`), `audience` (string/array — per-client default audience, FR-OIDC-6), `grantTypes` (⊆ `["authorization_code", "refresh_token"]`), `responseTypes` (`["code"]`), `requirePKCE` (default true), `skipConsent` (default **true**), `enableEndSession` (default true; requires `postLogoutRedirectUris`), `disabled`, `uri`, `icon`, `contacts`, `tos`, `policy`, `metadata`. Unknown fields rejected. Exact field mapping is frozen after risk R9 is verified. **`/admin/clients` states the consequence of `type` wherever it is chosen or shown**: the field says that only `web` keeps a secret and that changing the type is what issues or destroys one, registering a public client says why none appeared, and the table's type column reads "Public — no client secret" (**D78**).
*AC:* validation errors for duplicate `clientId`, public client with secret, confidential client without secret, `type: "service"` or a `client_credentials` entry in `grantTypes` (rejected as unsupported in v1, D26), undeclared scope/audience.

**FR-OIDC-4 — Endpoints.** `/oauth2/authorize`, `/oauth2/token`, `/oauth2/userinfo`, `/oauth2/introspect` (client auth; answers only for the caller's own tokens unless the client is flagged `resourceServer: true`), `/oauth2/revoke` (RFC 7009; always 200), `/oauth2/end-session`. `/oauth2/register` and the provider's own client/consent CRUD endpoints are unreachable (FR-OIDC-2). Client registration for administrators is this deployment's own `POST /api/auth/idp/create-client` · `/idp/update-client` · `/idp/rotate-client-secret` · `/idp/delete-client` · `/idp/set-client-disabled` (**D50**, **D72**): admin-gated, session-gated at the route — the session read authoritatively, past the cookie cache (**D81**) — validated with the `oauth_clients.json` entry schema, secret server-generated (≥ 32 chars) and returned once, hashed at rest, audit-logged, taken under the reconcile advisory lock, refusing a duplicate id and refusing to mutate a `user_id IS NULL` row. **`/idp/update-client` is a full replace of the create body and never changes the client id, the owning `user_id`, `disabled` or `created_at`**; the whole merged entry is re-validated, so a type change re-checks the stored redirect URIs; the stored secret hash is carried through unchanged for a client that stays confidential, minted and returned once when a public client becomes confidential, and discarded the other way; tokens and consents are revoked **only** when that public/confidential flip happens. **`/idp/rotate-client-secret` replaces the secret and revokes nothing** — hygiene, not incident response, with no grace window — and refuses a public client with `CLIENT_HAS_NO_SECRET` (**D72**). Unknown `client_id` or unregistered `redirect_uri` renders a branded error page and **never redirects**.

**FR-OIDC-5 — Always-JWT access tokens.** With an audience resolved (which v1 guarantees, FR-OIDC-6) every access token is a **signed JWT**; `jwt.algorithm ∈ {ES256 (default), RS256}` — any other value (incl. EdDSA/ES512/PS256) fails validation with a Neon-compatibility message. Header carries `alg` + `kid`.
*AC:* token from a plain code+PKCE login (no `resource` param) is a JWT that `jose` validates against the JWKS; the Neon constraints test (TST-4) passes.

**FR-OIDC-6 — Audience / resource model.** `jwt.audience` (string or array of absolute URIs) in `config.json` is **required** and is the default audience: applied as the RFC 8707 resource whenever a client sends no `resource` parameter (mechanism = risk R1). Per-client `audience` overrides the default; an explicit `resource` request is honoured when the client is linked to it. Effective resource registry = `oauth.resources[]` ∪ `jwt.audience` ∪ all per-client `audience`; reconcile seeds `oauth_resource` and links each client to the default plus its own audiences (risk R2); an unlinked/undeclared `resource` yields `invalid_target`.
*AC:* token without `resource` → `aud = jwt.audience`; token with allowed `resource` → that resource in `aud`; disallowed `resource` → `invalid_target`.

**FR-OIDC-7 — Access-token claims (one claims builder for all paths).** Payload: `iss` (= `baseUrl`), `sub` (= `user.id` — every v1 token is user-bound, D26), `aud` (FR-OIDC-6), `iat`, `exp`, `jti`, `scope`, `client_id`, `azp`, `sid` (session id; random for the API-key path), plus — when `jwt.includeUserData` (default true) — `email`, `name`, `given_name` (= firstName), `family_name` (= lastName), `roles` (string array, FR-ROLE-2), plus static claims from `jwt.claims` (FR-OIDC-8). `jwt.userClaims ⊆ {email, name, given_name, family_name, roles}` selects a subset; `includeUserData: false` removes exactly that set *(audit default)*. **Not** in access tokens: `email_verified`, `picture`, timestamps, ban/2FA fields. Which claims Better Auth auto-emits vs. the custom builder = risk R10.
*AC:* claim-set snapshot tests for: code+PKCE token, session-JWT and API-key JWT — all identical modulo `sub/sid/azp/scope`.

**FR-OIDC-8 — Static custom claims.** `jwt.claims: { name: value }`, default `{}` — e.g. `"role": "authenticated"` for Neon/Supabase (shipped in `config.example`, explained in the Neon guide; emitted **only when configured** *(audit default)*). Merged into access tokens; into ID tokens only when `jwt.claimsInIdToken` (default false). Reserved keys rejected by validation: `iss sub aud exp iat nbf jti scope auth_time azp sid client_id roles email name given_name family_name`.

**FR-OIDC-9 — Authorize flow & continuation.** The pending authorization request survives the interstitials in this order: login → status gate (pending/rejected/banned) → 2FA → forced password change → consent → redirect; held server-side ≤ 10 min within one browser session. Asynchronous approval does **not** resume the flow — the approval e-mail links to `/login` and the user restarts from the client. Supported request parameters: `prompt` (`none`, `login`, `consent`), `max_age`, `login_hint`, `nonce`, `state`, and `auth_time` is emitted. `prompt=none` yields `login_required`/`consent_required` without UI and only works for top-level/same-site callers (SameSite=Lax) — cross-site silent renew is unsupported; SPAs use refresh rotation. `request`, `request_uri` and `claims` parameters are unsupported and advertised as such.

**FR-OIDC-10 — Consent.** `skipConsent` defaults to true for all file clients (admin configured them); the consent page appears only for `skipConsent: false` clients or `prompt=consent`. Consent is stored per client + scope set and re-prompted on scope escalation; the consent POST is bound to the pending request and session; the page shows client name/icon/uri/tos/policy and per-scope descriptions. Users revoke consents in `/account` (revocation also revokes that client's tokens).

**FR-OIDC-11 — RP-initiated logout.** `/oauth2/end-session` per client (`enableEndSession`): `id_token_hint` validated; `post_logout_redirect_uri` must exactly match a registered value; `state` echoed; a confirmation page is shown when no valid hint is present — **this deployment's `/sign-out`**, never the provider's own unbranded HTML, which is intercepted and replaced (**D47**). Logout ends the IdP session; token revocation on logout per FR-AUTH-6. Front-channel logout is out of scope; back-channel logout is optional (only if the pinned Better Auth version supports it — else documented out).

**FR-OIDC-12 — Revocation semantics.** Ban, rejection, deletion, password change/reset, and admin "revoke all" revoke **all sessions and all OAuth refresh/access-token records** of the user; the `refresh_token` grant and API-key verification re-verify user state on every use. **Caveat (documented in spec + Neon guide):** already-issued JWT access tokens remain valid for stateless verifiers until `exp` — bounded by the access-token TTL (default 15 min); revocation is immediate at refresh/introspect/userinfo and all IdP endpoints.

**FR-OIDC-13 — Token lifetimes.** Defaults (all configurable, §6): authorization code 60 s · access token 15 min (per-resource override via `oauth.resources[].accessTokenTtl`) · ID token 1 h · refresh token 30 d sliding with 90 d absolute maximum, **rotation on every use with reuse detection** revoking the token family; refresh tokens require the `offline_access` scope; the refresh grant re-validates requested scopes against the client's *current* allowed scopes.

**FR-OIDC-14 — First-party apps & session-JWT endpoint.** A **first-party app** (`firstParty: true`) runs on the **same host** as the IdP — cookies are host-only, so only same-host apps share the session (the sub-path deployment exists precisely for this: `https://apps.example.com/app1` beside `https://apps.example.com/idp`); apps on other subdomains are not first-party and use the OIDC flow. `GET {baseUrl}/api/auth/token` with the session cookie returns a JWT from the same claims builder (`azp: "idp"`, `sid` = session id, `scope "openid profile email"`, `aud = jwt.audience`, TTL `jwt.sessionToken.ttl` = 3600 s); the endpoint is **same-origin only** (excluded from CORS) and refuses pending/banned users. The same endpoint's `x-api-key` mode is FR-KEY-3. (Risk R5.)

**FR-OIDC-15 — Discovery & well-known.** Served at the issuer: `{baseUrl}/.well-known/openid-configuration` and `{baseUrl}/.well-known/oauth-authorization-server`; under a sub-path the RFC 8414 document **additionally** lives at the origin root `{origin}/.well-known/oauth-authorization-server{path}` (the shipped Caddyfile adds that route). `issuer` equals `server.baseUrl` byte-for-byte; every advertised endpoint resolves; advertised: `scopes_supported`, `claims_supported`, `grant_types_supported`, `response_types_supported`, `code_challenge_methods_supported = ["S256"]`, `token_endpoint_auth_methods_supported`, `id_token_signing_alg_values_supported` (= configured alg), `end_session_endpoint`, `revocation_endpoint`, `introspection_endpoint`, `ui_locales_supported: ["en-US"]`. The advertised `jwks_uri` is **`{baseUrl}/.well-known/jwks.json`**; `{baseUrl}/api/auth/jwks` keeps answering byte-identically for anything that already found it (**D45**), `/.well-known/change-password` → `/account`, optional `security.txt` from config, `robots.txt` (disallow all).
*AC:* discovery content asserted against configuration in tests, at root **and** under a sub-path.

**FR-OIDC-16 — JWKS & key management.** Keys generated at first boot (serialized via advisory lock), stored in `jwks` with the private key AES-256-GCM-encrypted by `secret`. JWKS responses: `Cache-Control: public, max-age=300` + ETag; entries carry `kid`, `alg`, `use: "sig"`. Rotation: `jwt.rotationInterval` (default 90 d) + `jwt.gracePeriod` (default = max token lifetime + 1 h ≥ Neon's JWKS cache); retired keys stay published until grace ends; a new key must be published before it signs (risk R11); manual rotation via `idp rotate-keys` and the admin UI. The JWKS and discovery URLs must be reachable over **public https** for Neon — and because the advertised `jwks_uri` lives under `/.well-known/` (D45), exposing that one prefix publicly is enough, with the UI kept internal (documented).

**FR-OIDC-17 — CORS.** Discovery + JWKS: `Access-Control-Allow-Origin: *`. Token, revocation, userinfo: allow-list derived from the registered redirect-URI origins — **file clients ∪ enabled database clients**, cached process-locally and refreshed at start-up and by every client mutation (**D50**); disabled rows and non-browser (private-use scheme) redirects contribute nothing. Session endpoints and `GET /api/auth/token`: same-origin only.

### 5.10 E-mail (FR-MAIL)

**FR-MAIL-1 — Resend transport.** E-mail is sent via **Resend** (`email.resend.apiKey` placeholder, `email.from` required when enabled, `email.replyTo` optional). An internal transport abstraction has exactly two implementations in v1: `resend` and `capture` (tests/dev). The capture transport sends nothing and writes each message to a directory as one JSON file (**D30**). No SMTP in v1.
Template inventory (HTML + text, branded from `site.*`, links built from `baseUrl` only): verify e-mail · reset password · set password (admin-created account) · new pending sign-up (to admins) · account approved · account rejected (optional) · password changed · 2FA enabled/disabled · API key created. Templates are not user-customizable in v1.

**FR-MAIL-2 — Degraded mode.** Without `email.resend.apiKey`: password reset, e-mail verification and **all** notification e-mails are disabled; affected UI is hidden; a startup warning is logged; `auth.requireEmailVerification` is forced false; admins manage passwords via temporary password / one-time link (FR-ADMIN-2); accounts show an "unverified" badge (Q8).
*AC:* with e-mail off, `/forgot-password` is absent and the sign-up→approval flow still works end-to-end.

### 5.11 i18n (FR-I18N)

**FR-I18N-1 — Prepared, en-US only.** Every UI and e-mail string lives in one message catalog (`en-US`). Locale resolution: first supported of `ui_locales` (authorize request) → locale cookie → `Accept-Language` → `site.defaultLocale` (default `en-US`). Only the en-US bundle ships in v1; adding a locale must not require code changes outside the catalog. Dates render in the browser locale.

---

## 6. Configuration model (CFG)

**CFG-1 — Folder.** `IDP_CONFIG_DIR` (default `/config`), read-only to the process: `config.jsonc` (**required**), `oauth_clients.jsonc` (optional ⇒ no clients; only the first-party session-JWT path is usable), `roles.jsonc` (optional ⇒ built-in catalog, FR-ROLE-1), `branding/` (logo, favicon; served read-only). Each name is resolved `.jsonc` first, then `.json`; both spellings of one file present is a startup error naming both (**D60**). A mounted volume replaces any baked-in directory entirely. `config.example/` with fully commented examples ships in repo and image, and so does `config-schema/` with the generated JSON Schemas (`config.schema.json`, `oauth_clients.schema.json`, `roles.schema.json`), which is a sibling folder rather than part of the examples because the examples are copied to make a deployment's `config/`. Files are parsed as **JSONC** (comments, trailing commas); `$schema` is honoured and exempt from the unknown-key rule.

**CFG-2 — Placeholder grammar (D18).** Inside JSON **string values** only (never keys): `${env:NAME}`, `${env:NAME:-default}`, `${file:/abs/path}` (content read once, trailing newline trimmed — Docker/K8s secrets), `$${` escapes a literal `${`. `NAME` matches `[A-Z_][A-Z0-9_]*`. Single non-recursive pass, evaluated before schema validation; a value consisting of exactly one placeholder is coerced to the schema type by strict JSON parsing (`true`, `42`, `[...]`); embedded placeholders stay strings. Un-namespaced `${VAR}` and any malformed placeholder are errors (with a hint); an unresolved variable without default aborts startup naming file, JSON pointer and variable — never the value. Rationale (v0 asked for a common convention): compose/envsubst `${VAR}` collides with docker-compose's own interpolation and has no file source; the VS-Code-style namespace was chosen and extended with `file:`.

**CFG-3 — Precedence & env.** Effective value = config file (after substitution) → fallback env (only when the key is absent from the file: `BETTER_AUTH_URL`, `BETTER_AUTH_SECRET`, `DATABASE_URL`) → schema default. `DATABASE_URL_ADMIN` is the fallback env for `database.directUrl` (renamed from `DIRECT_DATABASE_URL`, **D48** — a clean break, no alias). `database.url` and `database.directUrl` are a pair of which **at least one** must be set, neither individually required; each falls back to the other, so a single-endpoint deployment may be configured under either name (**D74**). Env-only bootstrap variables (never in files): `IDP_CONFIG_DIR`, `HOST`, `PORT`, `LOG_LEVEL`, `LOG_FORMAT`, `IDP_MIGRATE_ON_BOOT`, `IDP_EMAIL_TRANSPORT`, `IDP_EMAIL_CAPTURE_DIR` (**D30** — a config-file key would be a copy-pasteable way to make a production deployment swallow every e-mail). There is **no** generic `IDP__SECTION__KEY` override mechanism.

**CFG-4 — Key inventory.** (type · default · req = required)

| Key | Type | Default | Notes |
|---|---|---|---|
| `server.baseUrl` | url | — **req** | Issuer; scheme+host[:port]+optional path, no trailing slash; https required in production (= non-localhost https rule below). |
| `server.host` / `server.port` | string/int | `0.0.0.0` / `3000` | Also env `HOST`/`PORT`. |
| `server.trustProxy` | bool \| CIDR[] | `false` | Honour `X-Forwarded-*` from the immediate upstream / listed ranges (rightmost-untrusted-hop). |
| `server.trustedOrigins` | url[] \| `*` | — | CSRF origin allow-list. Empty (the default) means the check **follows the request**: the browser's `Origin` must name the address the request arrived on — `X-Forwarded-Host`, else `Host` — as well as the issuer (**D68**). Setting it pins the check to what it lists; `https://*.example.com` is a pattern and `*` turns the check off. |
| `server.allowInsecureHttp` | bool | `false` | Permit non-https `baseUrl` outside localhost (dev only). |
| `server.shutdownTimeoutSeconds` | int | `10` | |
| `secret` | string | — **req** | ≥ 32 random bytes; fallback env `BETTER_AUTH_SECRET`; placeholder-only in production. |
| `database.url` | string | — **req** | Postgres connection string; fallback env `DATABASE_URL`; `sslmode` honoured. |
| `database.directUrl` | string | — | Connection string for every step that takes a **session advisory lock** — startup, migrations, `idp *`, the cleanup job. **Required when `database.url` points at a transaction-mode connection pooler** (Neon `-pooler`, PgBouncer): session locks do not hold through one (verified, S4/D27). Fallback env `DATABASE_URL_ADMIN` (**D48**); startup warns when the URL looks pooled and this is unset. |
| `database.schema` | string | `idp` | All IdP tables + drizzle migrations table live here; created on migrate; nothing in `public` (Q16, risk R8). Resolved at **runtime**: the Drizzle tables are built from this value and the migrator retargets the schema identifier in the committed SQL. |
| `database.ssl` | enum | `require` off-localhost | `disable\|require\|verify-full`; overrides URL `sslmode` when set. Unset, an explicit `sslmode` in the connection string wins over the not-localhost default; `prefer` and `allow` are ignored and fall through to it (**D40**). `database.sslCa` (PEM or `${file:}`). |
| `database.poolMax` | int | `10` | + `database.connectTimeoutSeconds`. |
| `database.migrateOnBoot` | bool | `true` | Also env `IDP_MIGRATE_ON_BOOT`. |
| `site.name` | string | — **req** | Branding; also TOTP issuer default. |
| `site.adminTitle` | string | `site.name` | What `/admin/*` calls itself, for a deployment whose people know it by another name (**D61**). Branding only — never the TOTP issuer, the e-mails or a token claim. |
| `site.logo` / `site.favicon` | path/url | — | Under `branding/` or URL. `logo.svg` and `branding/logo.svg` name the same file — the redundant prefix is dropped, not doubled (**D44**). |
| `site.supportEmail`, `site.termsUrl`, `site.privacyUrl` | string | — | |
| `site.theme` | enum | `system` | `system\|light\|dark` (built-in only). |
| `site.defaultLocale` | string | `en-US` | FR-I18N-1. |
| `site.nameFormat` | enum | `first-last` | `first-last\|last-first`. How the display name is composed from the captured parts (**D49**); never an input field. |
| `email.resend.apiKey` | string | — | Absent ⇒ degraded mode (FR-MAIL-2); placeholder. |
| `email.from` | string | req when e-mail on | `email.replyTo` optional. |
| `signUp.enabled` | bool | `false` | FR-SIGNUP-1. |
| `signUp.requireApproval` | bool | `true` | FR-SIGNUP-2. |
| `signUp.allowedEmailDomains` | string[] | `[]` | FR-SIGNUP-3. |
| `auth.defaultRedirect` | string | `/account` | Where a completed sign-in lands when nothing more specific applies (FR-AUTH-1, D28). Either a **same-origin relative path** (starts `/`, not `//` or `/\`, no `://`) or an **absolute http(s) URL on any origin** — a bare hostname is rejected. This is operator configuration, not user input, so a cross-origin value is not an open redirect; **SEC-3 is unchanged** and the runtime `returnTo` parameter stays same-origin-relative-only. Set it when the IdP is bundled beside the product it signs users in to. |
| `auth.requireEmailVerification` | bool | `true` | Forced false when e-mail off; password sign-in only. |
| `auth.password.minLength/maxLength` | int | `10`/`128` | `auth.password.breachCheck` bool, default false. |
| `auth.passwordReset.tokenTtlMinutes` | int | `60` | Verification link TTL 24 h. |
| `session.expiresIn` / `session.updateAge` | dur | `7d` / `1d` | |
| `session.cookieCacheMinutes` | int | `5` | ≤ 5. |
| `session.revokeOAuthTokensOnLogout` | bool | `false` | |
| `social.<provider>.*` | object | — | `enabled`, `clientId`, `clientSecret` (placeholder), `syncProfile` (true), `allowedEmailDomains`, provider options; `social.microsoft.tenantId` **required** (FR-SOC-5). |
| `twoFactor.enabled` | bool | `true` | `twoFactor.issuer` (default `site.name`), `twoFactor.trustDeviceDays` (30). |
| `apiKeys.enabled` | bool | `true` | `defaultExpiresIn` 365 d, `maxExpiresIn` 730 d, `tokenClientId` `"idp"`, `tokenTtl` 3600 s. |
| `jwt.algorithm` | enum | `ES256` | `ES256\|RS256` only. |
| `jwt.audience` | url \| url[] | — **req** | Default audience/resource (FR-OIDC-6); example = `baseUrl`. |
| `jwt.includeUserData` | bool | `true` | FR-OIDC-7. `jwt.userClaims` subset selector. |
| `jwt.claims` | object | `{}` | Static claims (FR-OIDC-8); `jwt.claimsInIdToken` bool, default false. |
| `jwt.rotationInterval` / `jwt.gracePeriod` | dur | `90d` / max-token-lifetime + 1 h | FR-OIDC-16. |
| `jwt.sessionToken.ttl` | int s | `3600` | FR-OIDC-14. |
| `oauth.accessTokenTtl` | dur | `15m` | `oauth.idTokenTtl` 1 h, `oauth.codeTtl` 60 s, `oauth.refreshTokenTtl` 30 d, `oauth.refreshTokenMaxLifetime` 90 d. (`oauth.m2mAccessTokenTtl` removed by D26.) |
| `oauth.scopes` | string[] | `[openid, profile, email, offline_access]` | Clients may only reference declared scopes. |
| `oauth.resources` | (url \| object)[] | `[]` | Optional extra resources: `{ identifier, name?, allowedScopes?, accessTokenTtl? }`. |
| `oauth.reconcile.prune` | bool | `false` | FR-OIDC-2. |
| `admin.adminRoles` | string[] | `["admin"]` | Must exist in the catalog. |
| `admin.allowImpersonation` | bool | `false` | FR-ADMIN-5. There is no `admin.bootstrap`: it was removed with the env bootstrap (**D52**), and an unknown key is a start-up error, so an upgrade that still declares it is told. |
| `rateLimit.enabled` | bool | `true` | `rateLimit.storage: "database" (default) \| "memory"`; per-endpoint rules (SEC-2). |
| `logging.level` / `logging.format` | enum | `info` / `json` | `json\|pretty`; env `LOG_LEVEL`/`LOG_FORMAT`. |
| `cleanup.intervalMinutes` | int | `60` | OPS-8. |
| `audit.retentionDays` | int | `90` | SEC-6. |

**CFG-5 — Validation.** All three files are validated together against zod schemas (exported as JSON Schema to `config-schema/` for editor IntelliSense); `additionalProperties: false`; **all** errors reported in one pass, each naming the file *as it is spelled on disk* (**D60**). Cross-checks (startup errors): duplicate `clientId`/role names · public client with secret / confidential without · secret < 32 chars · `type: "service"` or `client_credentials` in `grantTypes` (unsupported in v1, D26) · empty `redirectUris` on an authorization-code client · invalid/unregistered URIs · undeclared scopes/resources · missing or duplicate `default` role · `adminRoles` entry missing from the catalog · `jwt.algorithm` outside {ES256, RS256} · `tenantId` in {common, organizations, consumers} · **literal (non-placeholder) secrets in production** (production = https `baseUrl`) for `secret`, client secrets, social secrets, `email.resend.apiKey`. `idp config validate` prints the effective configuration with secrets masked and exits non-zero on failure. Configuration is read **once**; changes require a restart (no hot reload, SIGHUP ignored).

**CFG-6 — Secrets.** `secret` ≥ 32 random bytes (generation command documented); rotating it invalidates all sessions **and makes the AES-GCM-encrypted `jwks` private keys undecryptable** — the runbook (DOC-4) covers re-keying (`idp rotate-keys`) and notifying verifiers. Docker builds never receive secrets via `ARG`/`ENV`; images contain no secrets; `${file:}` supports Docker/K8s secret files; secrets never appear in logs (SEC-5).

---

## 7. Data model (DM)

**DM-1 — Authoritative schema.** Generated from the **installed** Better Auth's `getAuthTables()` for the enabled plugins by `apps/web/scripts/generate-auth-schema.ts`, not by the `auth` CLI (**D29**); custom fields are declared as Better Auth `additionalFields` and the custom `audit_log` table as the schema of a small **local Better Auth plugin**, so one pass emits everything (v0's "all ORM from the plugin set" holds). The two read the same source of truth and produce the same seventeen tables field-for-field; they differ only in the wrapper, and that difference is the point — the CLI emits module-level constants with the schema name baked in as a string literal, while a runtime `database.schema` (CFG-4, D27, DM-4) needs a `createAuthSchema(schemaName)` factory plus `CANONICAL_SCHEMA_NAME` for the migrator to retarget. Then `drizzle-kit generate` produces committed SQL migrations. CI fails on drift between the committed schema and a fresh generator run.

**DM-2 — Tables in scope.** `user`, `session` (keeps `impersonated_by`; drops `active_organization_id`), `account`, `verification`, `two_factor`, `rate_limit`, `jwks`, `oauth_client`, `oauth_refresh_token`, `oauth_access_token`, `oauth_consent`, `oauth_resource` + `oauth_client_resource` (1.7 names per generator), `oauth_client_assertion` (the used-`jti` window that makes a client assertion single-use), `pending_authorization` (emitted by the generator; **nothing writes to it** — D33 — and the cleanup job purges it regardless), `apikey`, custom `audit_log` — the seventeen of DM-1. **Out:** `organization`, `organization_role`, `member`, `invitation`, `subscription`, and `user.stripe_customer_id`.

**DM-3 — `user` columns (D13).** From the v0 reference, kept with these names: `id, name, first_name, last_name, email (unique), email_verified, image, created_at, updated_at, role, banned, ban_reason, ban_expires, two_factor_enabled` — plus custom `status` (`pending|active|rejected`), `approved_at`, `approved_by`, `must_change_password`. Claim mapping per FR-OIDC-7/§5.7.

**DM-4 — Schema placement.** Everything (including drizzle's migrations bookkeeping table) lives in `database.schema` (default `idp`); nothing in `public` (risk R8).

**DM-5 — Retention.** Expired sessions, verification rows, auth codes, expired/revoked tokens (> 30 d), expired `oauth_client_assertion` JTIs (the replay window is over), expired `pending_authorization` rows (swept unconditionally, so D33's "written by nothing" cannot become a leak if that ever changes), stale rate-limit rows and retired JWKS keys past grace are purged by the cleanup job (OPS-8); `audit_log` rows per `audit.retentionDays`.

---

## 8. Security requirements (SEC)

**SEC-1 — Absolute URLs.** Every absolute URL (issuer, discovery, e-mail links, social callbacks, redirects, cookies) derives from `server.baseUrl` only — never from `Host`/`X-Forwarded-Host`. *AC:* host-header injection test (TST-5).

**SEC-2 — Rate limiting.** Enabled by default; storage `database` (survives restarts, replica-ready). Stricter documented rules for: sign-in, sign-up, forgot/reset password, verify e-mail, 2FA attempts, `/oauth2/token` (per client id **and** per IP), `/oauth2/authorize`, API-key verification. Client IP honours `server.trustProxy` (bool or CIDR list, rightmost-untrusted-hop); spoofed-header behaviour tested. 429s show retry-after without revealing thresholds.

**SEC-3 — CSRF & redirects.** Better Auth origin checks with `server.trustedOrigins`; every state-changing endpoint — including TanStack server functions — enforces them. With no allow-list configured the check is against the address the request arrived on rather than the issuer alone (**D68**), which is a check a cross-site page still cannot pass; `advanced.disableOriginCheck: false` is set explicitly so the test runs exercise it, because Better Auth turns it off under `NODE_ENV=test`. Any `returnTo`/`callbackURL` parameter must be a same-origin relative path; OAuth redirects only to registered URIs (FR-OIDC-3/4).

**SEC-4 — Headers.** HTML: CSP without third-party hosts, `frame-ancestors 'none'` / `X-Frame-Options: DENY` (login/consent clickjacking), `Referrer-Policy: strict-origin-when-cross-origin`, `X-Content-Type-Options: nosniff`; HSTS when https (app or Caddy — documented). `form-action` is `'self'` **plus every registered client's redirect and post-logout origin** — the FR-OIDC-17 list, which since **D50** includes enabled database clients — because a completed authorization is a 303 from a form to the client (**D46**). `/oauth2/token`, `/oauth2/userinfo`, `/oauth2/introspect`: `Cache-Control: no-store`.

**SEC-5 — Logging & redaction.** Structured JSON to stdout with request id, method, path, status, duration, anonymized IP. Never logged: passwords, tokens, authorization codes, secrets, reset/verification links, `Authorization`/`Cookie` headers, `/oauth2/*` and `/api/auth/*` query strings. Log level/format per config.

**SEC-6 — Audit log.** Append-only `audit_log` table (actor, target, action, outcome, ip, ua, requestId, metadata, createdAt) **and** stdout events for: sign-in success/failure, **sign-up** (`signup.created`, self-service only) and **account creation** (`user.created`, by an administrator or the first-run wizard, with `by`/`via` in the metadata — **D66**), verification, approval/rejection, ban/unban, role change, password change/reset (requested/completed), session revocation, 2FA enrol/disable/reset, API-key create/revoke/failure, impersonation start/stop, consent grant/revoke, token issuance (`client_id`, `sub`, scopes), token revocations, client reconciliation diffs, **client create/update/secret rotation/delete/disable from the admin UI** (**D50**, **D72**), **admin profile edits** (`user.profile_changed`), key rotation, `social.profile_conflict`. An API-key event's target is always the **key** (`targetType: "apikey"`), with the owner in `metadata.userId`, wherever it was written from. **Every `/admin/*` endpoint's row is written by `admin/guard.ts`** — and so is the OAuth revocation that goes with an action ending someone's access (**D67**) — so both are the same whether the caller was a button or a `curl` (FR-ADMIN-6); `auth/options/hooks.ts` owns Better Auth's own surface, and a manual write in a route survives only where no hook can see the event (**D66**). Browsable at `/admin/audit`; retention `audit.retentionDays`; no secrets stored.

**SEC-7 — Anti-enumeration.** Uniform responses for forgot-password, resend-verification and sign-in failures; sign-up enumeration mitigated by rate limits and (when e-mail is on) verification-first flow; documented residual risk.

**SEC-8 — Self-contained runtime.** No third-party origins at runtime besides Postgres, Resend and enabled social providers: fonts and assets bundled; CSP allows no external hosts; egress needs documented (DOC-4).

**SEC-9 — Supply chain.** All dependencies pinned exactly (no `latest`), committed lockfile, `pnpm audit` in CI (nightly), image scanned (Trivy) with SBOM attached; base image pinned by digest.

**SEC-10 — Better Auth security defaults respected.** Password hashing = Better Auth default (scrypt); `storeClientSecret: "hashed"`; JWKS private keys encrypted at rest; no debug endpoints in production.

---

## 9. Operations (OPS)

**OPS-1 — Image.** Multi-stage build from pinned `oven/bun:<version>-slim`; `pnpm install --frozen-lockfile`; final stage: production output only, non-root user, read-only root filesystem, `/config` mounted read-only, only `/tmp` writable, `TZ=UTC`; amd64 + arm64. Tags: `X.Y.Z`, `X.Y`, `X`, `latest`, immutable `sha-<git>`; OCI labels; `CHANGELOG.md`; migrations applied automatically on upgrade; no downgrade support ("back up before upgrading"). Published by [`.github/workflows/release.yml`](.github/workflows/release.yml) — and only by it — on a `vX.Y.Z` tag whose version matches the workspace's, after the TST-8 smoke test, the scan and the SBOM have run against the image being pushed; a pre-release tag takes neither `X.Y`, `X` nor `latest` (**D73**).

**OPS-2 — Startup sequence.** load + validate config → connect DB → migrate (when `database.migrateOnBoot`) → ensure signing key → reconcile clients/resources → refresh the client-origin cache (**D50**) → validate roles vs. DB → first-run check (**D52**: log where to finish setup when the `user` table is empty; create nothing) → listen → ready. Every shared-state step runs under a Postgres advisory lock (replica-safe; single instance is the supported topology). Any failure exits non-zero with **one** actionable error.

**OPS-3 — Health.** `GET {baseUrl}/healthz` (alive, version) and `GET {baseUrl}/readyz` (config loaded, DB reachable, migrations current, signing key present): unauthenticated, excluded from rate limiting and request logs, non-revealing. Dockerfile `HEALTHCHECK` calls `http://127.0.0.1:$PORT{path}/healthz` via Bun (no curl in slim images).

**OPS-4 — Shutdown.** On SIGTERM/SIGINT: stop accepting, drain ≤ `server.shutdownTimeoutSeconds` (10 s), close pool, exit 0; Bun runs as PID 1; compose sets `stop_grace_period`.

**OPS-5 — Migrations.** Dev chain per DM-1. Runtime: `migrate()` on boot under advisory lock (default) or explicitly via `idp migrate`; forward-only.

**OPS-6 — Operator CLI.** `idp config validate` · `idp migrate` · `idp reconcile-clients` · `idp rotate-keys` · `idp cleanup` · `idp version` — all runnable as `docker run <image> idp <cmd>` against the same config folder/env, and as `docker/idp-cli.sh <cmd>` (or `idp-cli.cmd`) against a running stack. `idp reset-admin` is **removed** with the bootstrap it recovered (**D52**).

**OPS-7 — Reference deployment.** Everything deployment-related lives in `docker/` (**D51**): `Dockerfile` + `Dockerfile.dockerignore`, `docker-compose.yml`, `docker-compose.e2e.yml`, both Caddyfiles, and a `.cmd`/`.sh` pair per lifecycle verb — `idp-create` · `idp-start` · `idp-stop` · `idp-status` · `idp-logs` · `idp-cli` · `idp-destroy` — invoked from that folder with `--env-file ../.env`. The compose file: `build: { context: .., dockerfile: docker/Dockerfile }`, pinned Postgres (16/17), named volume, healthchecks, `depends_on: condition: service_healthy`, `../config:/config:ro`. **The database URLs arrive whole through `env_file`** (**D48**) — `DATABASE_URL`, and `DATABASE_URL_ADMIN` when pooled — which cannot be overridden by a host shell value and so keeps D43's protection while the variables move; `IDP_CONFIG_DIR` and `IDP_SCHEMA_NAME` stay pinned in `environment`. Nothing is assembled from a password: `POSTGRES_PASSWORD` is an optional, defaulted knob of the bundled `expose:`-only reference database, and the docker `secrets:` block is gone. Optional **Caddy** profile — never in the image (automatic HTTPS; Caddyfiles for host-root **and** sub-path incl. the origin-root RFC 8414 route). No Mailpit (Resend-only); dev/test e-mail uses the capture transport. Root `package.json` exposes `docker:build` · `docker:up` · `docker:down` · `docker:smoke`.

**OPS-8 — Cleanup.** In-process job every `cleanup.intervalMinutes` (jitter + advisory lock) purging per DM-5; `idp cleanup` on demand; counts logged.

**OPS-9 — Proxy & reachability.** Reference Caddyfile ships; `server.trustProxy` must be enabled behind it; `/.well-known/*` and the JWKS URL must be reachable over public https for Neon; social callbacks require browser reachability of `baseUrl`. **`server.baseUrl` must also resolve from inside the deployment itself**: RP-initiated logout verifies `id_token_hint` against the key set fetched from it (**D47**), so a split-horizon DNS or a loopback-only publish degrades that flow to a confirmation prompt. Standalone plain-HTTP only with `server.allowInsecureHttp` (dev).

**OPS-10 — Sub-path deployment (D2).** `baseUrl` may carry a path; all routes, assets, cookies (`Path`), e-mail links, callbacks and discovery honour it; Caddy proxies `{path}/*` **without stripping** and adds the origin-root RFC 8414 route. (Risk R3.)

**OPS-11 — Single instance.** v1 is documented and tested single-instance; because all mutating startup steps take advisory locks and rate limiting is DB-backed, accidental replicas are safe but unsupported.

**OPS-12 — Backups.** Out of scope; README states that `jwks` holds the (encrypted) signing keys and that losing the DB or `secret` invalidates every issued token (verifiers must refetch the JWKS after re-keying).

**OPS-13 - Footprint targets.** Idle RSS < 256 MB; ready < 5 s excluding migrations; final image < **350 MB** - measured in the CI smoke test, and **only** there: Docker Desktop's containerd image store answers `docker image inspect --format {{.Size}}` with the compressed size, so the same image reads 117.9 MiB locally and 385.8 MiB on a runner (**D76**). The ceiling was 300 MB and had never been measured, because no workflow in this repository had ever run; the first one that did found the image over it. Raised to 350 MB *and* the image slimmed to about 295 MB on amd64 - `pnpm deploy --legacy` ignores `--prod`, so a compiler, a formatter, a test runner and three esbuild binaries were shipping in the runtime image and are now removed by name.

---

## 10. Testing requirements (TST)

**TST-1 — Layers & gates.** Unit (Vitest) · integration against **real Postgres** (testcontainers or CI service container — PGlite is not a substitute for the postgres driver) · e2e (Playwright) against the **built Docker image** (real Bun runtime) with the capture-transport for e-mail — switched on by the env-only `IDP_EMAIL_TRANSPORT=capture`, writing one JSON file per message under `IDP_EMAIL_CAPTURE_DIR` (default `/tmp/idp-mail`, the image's only writable path), which the run mounts and reads from the host; captured mail has no HTTP surface (**D30**) · container smoke test. Turbo tasks `test`, `test:integration`, `test:e2e`; CI (GitHub Actions) on every PR: lint, typecheck, unit, integration, e2e, docker build + smoke, schema drift (DM-1), example-config validation, nightly `pnpm audit` — all required for merge. Coverage: ≥ 85 % for config/claims/reconcile/approval modules, ≥ 70 % overall. Deterministic seeds; fresh schema per integration file; `config.test/` fixtures.

**TST-2 — Unit scope.** Config loader (schema errors, unknown keys, aggregated reporting, JSONC, `$schema` exemption, `.jsonc`/`.json` resolution and the both-present refusal), placeholder grammar (env/file/defaults/escape/coercion/unresolved/no-recursion/un-namespaced rejection), client + role validation rules (every CFG-5 cross-check), role catalog compilation, claims builder (FR-OIDC-7/8 permutations), approval state machine.

**TST-3 — Lifecycle integration.** Sign-up with approval on (pending → login refused → approve → login) and off; sign-up disabled (4xx + hidden UI + social refusal); verification (capture transport, expiry, resend); reset (reuse refused, sessions + OAuth tokens revoked); banned user (login + refresh + API key refused); admin flows (create/edit/roles/ban/approve/reject/revoke/delete/2FA reset); bootstrap idempotency; reconciliation (create/update/secret change/disable/prune/invalid file aborts; CRUD endpoints denied); 2FA (enrol, login, backup code, trust device); API keys (create/use/expire/revoke/exchange).

**TST-4 — Protocol integration.** Discovery correctness at root **and** sub-path (issuer equality, endpoints, S256, alg, `ui_locales_supported`); full code+PKCE flow **without `resource`** → JWT with `aud = jwt.audience`, verified via `jose` `createRemoteJWKSet` (alg ES256, `kid`, `iss`, `aud`, `exp`, `sub`, user claims, `roles`, static `role` when configured); explicit allowed/disallowed `resource`; ID-token claim set by scope; userinfo; refresh rotation + reuse detection; revocation; introspection authz; end-session (hint/no-hint); `client_credentials` rejected with `unsupported_grant_type` and absent from discovery (D26); session-JWT and API-key-JWT paths (same claims); **Neon constraints test** (ES256/RS256, `kid` header, `use: sig`, `sub`, `exp`); JWKS rotation grace behaviour; full RP login through a minimal sample relying party (`openid-client`) kept in the repo.

**TST-5 — Security tests.** Approval gate on every path; mass assignment (FR-AUTH-7); redirect-URI mismatch renders error without redirect; PKCE downgrade rejected; wrong/missing client secret; public client sending `client_secret_*`; code replay revokes; CSRF origin checks incl. server functions; `/admin/*` and admin API reject non-admins; rate limits trigger (incl. spoofed `X-Forwarded-For` in standalone mode); host-header injection changes nothing; uniform forgot-password responses; last-admin protection; impersonation off by default; cookies carry the FR-AUTH-5 attributes.

**TST-6 — E2E (Playwright).** Login (success/failure/banned/pending), sign-up on/off, verification + reset via capture transport, 2FA enrol + challenge, consent approve/deny, end-session, account page (password change, sessions, API keys shown in a one-shot dialog with the secret never in the URL), admin (list/search/pagination, approve/reject, roles as checkboxes, ban, client create/disable/remove), the **first-run setup wizard** — each stack is taken through it in a real browser before any spec runs, and the gate closing afterwards is asserted (**D52**), complete OIDC login via the sample RP — run at host root **and** behind Caddy under a sub-path; axe checks with zero serious/critical violations on every page.

**TST-7 — Social & release checks.** Automated social path via a mock OIDC provider in test config; documented manual checklist against Google/GitHub/Entra before each release; release checklist item: validate one real token against a real Neon project.

**TST-8 — Container smoke test.** Compose up (from `docker/`, against a generated config folder **and** a generated `.env`) → `/readyz` → discovery + JWKS fetch → the **first-run setup wizard**, scripted, including the `/` → `/setup` redirect and the gate closing afterwards (**D52**) → one login → one token issuance → SIGTERM → clean exit code; asserts OPS-13 targets.

---

## 11. Documentation requirements (DOC)

**DOC-1 — README** (replaces the template text): what it is + non-goals · features **and benefits** · architecture (IdP, Postgres, Caddy, client apps, Neon as verifier) · quick start with `docker/idp-create.sh` (generate secret, first-run setup page, first login — executed in CI so it cannot rot) · configuration reference **generated from the zod schemas** (CI fails when stale) · `oauth_clients.json` / `roles.json` references with examples · well-known endpoint list · reverse proxy & sub-path guide (Caddyfiles) · e-mail (Resend) setup · social provider setup with exact callback URLs (incl. Entra tenant requirement) · security notes · operations runbooks (DOC-4) · troubleshooting (`invalid_redirect_uri`, issuer mismatch, EdDSA-rejected-by-Neon, secure cookies over http, unresolved placeholders, migration lock, Resend failures) · development & testing · versioning/changelog · license.

**DOC-2 — Neon guide.** JWKS URL to register ( `{baseUrl}/.well-known/jwks.json`, the advertised `jwks_uri` — D45 ), required algorithm (ES256/RS256), `sub` → `auth.user_id()`, the `roles` array vs. static `role: "authenticated"` claims, audience configuration (`jwt.audience` ↔ Neon's expected audience; Neon checks `aud` only when configured), public JWKS reachability, key-rotation grace vs. Neon's ≤ 1 h JWKS cache, revocation caveat (FR-OIDC-12), a minimal RLS policy example.

**DOC-3 — Client registration guide.** Worked examples: public SPA (PKCE, `none`), confidential server app (`client_secret_basic`), first-party app (session JWT), generic `openid-client` setup — plus an explicit note that machine-to-machine (`client_credentials`) is not supported in v1 and that a per-user API key (FR-KEY-3) is the v1 answer for scripted access — each with its `oauth_clients.jsonc` entry, secret placeholder, and how the default audience / explicit `resource` works.

**DOC-4 — Runbooks & supporting files.** Upgrade (pull, migrate-on-boot, rollback policy) · key rotation · `secret` rotation (CFG-6 consequences) · client reconciliation · cleanup · reading audit logs · egress requirements. Files: `SECURITY.md` (disclosure contact), `CONTRIBUTING.md` (pnpm/Bun/CLI-generate/migrations/test commands), `CHANGELOG.md`, `config.example/`, `config-schema/`, sample Caddyfiles, compose file.

---

## 12. Decision log

### 12.1 Owner decisions (2026-08-23)

| # | Decision | Landed in |
|---|---|---|
| D1 | Better Auth latest (1.7.1 at spec time); v0 schema = names only | §4, DM-1 |
| D2 | Sub-path deployment required | OPS-10, FR-OIDC-15, TST-4/6 |
| D3 | "API key" = both `client_credentials` and per-user keys | FR-OIDC-1, FR-KEY — **superseded by D26**: v1 has per-user keys only |
| D4 | 2FA (TOTP) in v1, optional per user | FR-2FA |
| D5 | Roles = catalog only; admin area admin-only; roles are claims | FR-ROLE |
| D6 | Files are source of truth; DB rows are the reconciled operative store | FR-OIDC-2 |
| D7 | Always JWT access tokens, Neon-valid; ES256 default | FR-OIDC-5/6 |
| D8 | No account linking; identity = provider subject; providers via config | FR-SOC |
| D9 | Consent optional; file clients skip by default | FR-OIDC-10 |
| D10 | Resend API key; absent ⇒ e-mail features disabled | FR-MAIL |
| D11 | Sign-up: global, default off; approval default on | FR-SIGNUP |
| D12 | ~~Bootstrap admin in config.json via env placeholders~~ — **superseded by D52** | FR-ADMIN-1 |
| D13 | v0 user columns kept; user data in JWT by default | DM-3, FR-OIDC-7 |
| D14 | Custom static claims via config (`role: "authenticated"`) | FR-OIDC-8 |
| D15 | OAuth tokens cleared on password change (+ ban/reject/delete) | FR-OIDC-12 |
| D16 | Audience via config.json | FR-OIDC-6 |
| D17 | i18n prepared, en-US only | FR-I18N |
| D18 | `${env:NAME}` placeholder grammar | CFG-2 |
| D19 | `database.url` + `database.schema` (default `idp`) | CFG-4, DM-4 |
| D20 | Access-token claim set (from the owner's token doc; ES256 instead of EdDSA) | FR-OIDC-7 |
| D21 | Ban kept (OOTB feature) | FR-ADMIN-4 |
| D22 | Entra identity via `oid`; tenantId mandatory | FR-SOC-2/5 |
| D23 | Logout endpoint clears cookie; first-party = same-host client | FR-AUTH-6, FR-OIDC-14 |
| D24 | Social profile-sync e-mail collision **blocks the sign-in** (neutral refusal + `social.profile_conflict`), rather than silently skipping the update | FR-SOC-4 |
| D25 | **No** startup warning for "social enabled while sign-up off"; the "unverified open registration" warning stays | FR-SIGNUP-4 |
| D27 | **`database.directUrl`** added: every advisory-locked step (startup, migrations, CLI, cleanup) uses it, and it is required whenever `database.url` is a transaction-mode pooler. Forced by spike S4, which showed session locks do not hold through Neon's `-pooler` endpoint while they do through the direct one | CFG-4, OPS-2/5/8, §13 R8 |
| D28 | **`auth.defaultRedirect`** added (default `/account`): the post-sign-in destination is operator-configurable as a same-origin relative path or an absolute URL on any origin, sitting third in the FR-AUTH-1 precedence behind an OAuth continuation and a validated `returnTo`. Forced by review finding R-3 — the destination was hard-coded to `/account`, which is wrong whenever the IdP is bundled beside its product, and 404s until M7 ships. The resolver also governs password-change completion, since an absolute default cannot survive the `returnTo` round trip FR-AUTH-4 relies on. SEC-3 unchanged | FR-AUTH-1, FR-AUTH-4, CFG-4, SEC-3 |
| D29 | The Drizzle schema stays generated by `apps/web/scripts/generate-auth-schema.ts` rather than the Better Auth CLI, and the recorded justification is corrected. The old reason — a CLI "version-stranded at 1.4.21" — was **false**: `@better-auth/cli` is deprecated and the CLI was renamed to `auth`, which publishes 1.7.1 against `better-auth@1.7.1` and `@better-auth/core@1.7.1`, our exact pins. Run against a shim of our own option set it emits the same 17 tables, and after two defects it exposed were fixed the two outputs are field-, index- and FK-identical. It survives for a structural reason instead: the CLI emits module-level constants with the schema name baked in as a string literal, and a runtime `database.schema` (D27) needs a `createAuthSchema(schemaName)` factory the CLI has no code path to produce | §4, DM-1, R-2 |
| D30 | **Captured e-mail is a directory of files, not an endpoint.** The `capture` transport (FR-MAIL-1) additionally writes each message to disk as one JSON file when the env-only `IDP_EMAIL_TRANSPORT=capture` is set, under `IDP_EMAIL_CAPTURE_DIR` (default `/tmp/idp-mail` — OPS-1 leaves `/tmp` as the only writable path); the Playwright run mounts that directory and reads the verification and reset links from it. Both variables are CFG-3 env-only: a `config.json` key would be a durable, copy-pasteable way to turn a production deployment into one that silently swallows every password-reset e-mail. It is honoured **only when e-mail would otherwise work**, so FR-MAIL-2's degraded mode stays testable. The alternative — an HTTP inbox in the image — would have put a read-any-mail endpoint in the shipped artefact to serve a test | FR-MAIL-1, CFG-3, TST-1, TST-6 |
| D32 | **`aud` on an access token is an array containing `jwt.audience`, not the bare string.** Spike S1 planned to normalise it away in the `jwt.sign` seam; 1.7.1 makes that impossible — the `jwt` plugin *refuses to construct* with `jwt.sign` unless `jwks.remoteUrl` is also set (`index.mjs:24`), which would move the whole key set off this deployment. With `scope=openid` the provider always appends its own `{authBaseUrl}/oauth2/userinfo` as a second audience and offers no option to suppress it, so a conformant token carries both values. Every RFC 7519 §4.1.3 verifier — `jose`, Neon, PostgREST — checks `aud` by membership, so nothing downstream changes; FR-OIDC-6's acceptance criterion is read as "the token's audience **includes** `jwt.audience`". The alternative was patching `@better-auth/oauth-provider`'s `resolveResourcePolicy`, which was judged the worse trade for a claim shape that is already correct | FR-OIDC-6, S1, R1 |
| D33 | **The interrupted authorization needs no server-side store.** FR-OIDC-9 was planned around the `pending_authorization` table plus a host-only handle cookie. `@better-auth/oauth-provider@1.7.1` already carries the whole authorization request in the interstitial page's query string, signed with the server secret and stamped with an expiry, and re-runs it from `POST /oauth2/continue`. The table stays in the generated schema — removing it would be a migration for no gain, and M12's cleanup job purges it either way — but nothing writes to it. This deployment still *drives* the resume rather than letting the provider's automatic after-hook do it: that hook fires whenever a request carrying the signed query sets a session cookie, which would hand an authorization code to a user with a temporary password before FR-AUTH-4's forced change | FR-OIDC-9, FR-AUTH-4 |
| D34 | **The last-administrator rule is checked before the self-action rules.** When both fit, "give another account an admin role first" is the answer, not "ask another administrator". The ordering was the other way round for a day, on the reasoning that the self message is more actionable — which is exactly backwards: with two administrators the last-admin rule never applies, so that message was only ever shown to the one person who had nobody to ask | FR-ADMIN-3, FR-ROLE-3 |
| D35 | **An admin endpoint accepts a session built by the api-key plugin.** `getAuthoritativeSessionFromCtx` re-reads the session row past the cookie cache by nulling `context.session` and reading the cookie again, so an API-key caller — who has no cookie — was answered 401 by every administrative endpoint, making the documented management interface of FR-ADMIN-6 unreachable to its intended callers. The gate now falls back to the session the hooks resolved. That session is not the staler of the two: the FR-KEY-2 gate re-reads the owner's standing on every use, sooner than any cookie session is re-checked | FR-ADMIN-6, FR-KEY-2, FR-AUTH-5 |
| D36 | **The CSP concedes `script-src 'unsafe-inline'`, and contains the concession elsewhere.** TanStack Start streams the HTML shell and emits the router's serialised state as inline `<script>` blocks the application cannot reach, so there is no seam for a per-request nonce and hashing is impossible for a payload that differs on every request. The rest of the policy is written so that this is the only concession: `default-src 'self'` with no remote origin anywhere, `connect-src 'self'` so an injected script has nowhere to send what it steals, `form-action 'self'` so it cannot move the sign-in form's action, plus `object-src`/`frame-src 'none'` and `base-uri 'self'`. This is materially weaker than a nonce and is recorded as such; `server/http/security-headers.ts` is the one place to change when Start grows a nonce hook | SEC-4 |
| D37 | **The per-client-id half of SEC-2's `/oauth2/token` rule is implemented outside Better Auth.** 1.7.1 builds every rate-limit key as `ip:path` and `rateLimit.customRules` can only change a path's window and maximum, never its key — so a per-client bucket cannot be expressed through it. Rather than escalate this as a SEC-2 deviation, the bucket is implemented in `routes/oauth2/token.ts` over the **same `rate_limit` table** with a prefixed key (`server/http/rate-limit.ts`), reading the client id from `Authorization: Basic` as well as the form body. Both halves apply: Better Auth's per-IP rule and this per-client one. Per IP alone is the wrong shape for this endpoint — a confidential client behind one NAT is a single address for its whole user base | SEC-2, FR-OIDC-4 |
| D38 | **The private `x-idp-socket-address` header, always overwritten at the edge.** `Bun.serve` reports the socket address and a `Request` does not carry it, so the edge stamps it into a header that `clientIpFrom` and Better Auth's `advanced.ipAddress.ipAddressHeaders` both read. Setting `ipAddressHeaders: []` for `trustProxy: false` — the previous behaviour — left Better Auth unable to resolve any address, so **every caller shared one rate-limit bucket**, which is either too wide to limit anything or narrow enough to lock a whole deployment out at once. The header is overwritten and never merged, so a client sending one of its own has it erased before anything reads it | SEC-2, SEC-1 |
| D31 | **The site does not need to work without JavaScript.** Progressive enhancement stays where it is free — the public pages are still plain server-rendered `<form method="post">`, so the first paint is correct before hydration — but no feature has to remain usable with scripting off, and R-1's "still works with JavaScript disabled" acceptance criterion is withdrawn. Prompted by the measurement under R-1: Chromium and WebKit clamp `-webkit-text-security` back to `disc` on a password field, so the scriptless reveal the code claimed only ever worked in Firefox | FR-ACCT-2, R-1 |
| D26 | **No machine-to-machine support in v1**: the `client_credentials` grant, the `service` client type, `clientCredentialsScopes` and `oauth.m2mAccessTokenTtl` are removed; "API keys" = per-user keys (FR-KEY) only | §1.2/1.3, §2, §3, FR-OIDC-1/3/7, CFG-4, CFG-5, SEC-6, TST-4, DOC-3, supersedes D3 |
| D39 | **Vite's `base` is relative for the build only.** One build has to relocate to any mount path (OPS-10, spike S3), which `base: "./"` gives for chunk-to-chunk imports and CSS `url()`. Setting it unconditionally is what broke development: Vite 8 stopped coercing a relative base to `/` for the dev server and now prefixes every transform URL with `/./`. A real path survives that; Vite's own internal URLs are matched by their `/@…` prefix and do not, so `/./@fs/…`, `/./@id/…` and `/./@vite/client` all resolved to null. The page still server-rendered, so nothing errored — it arrived with no stylesheet, no client entry and no HMR, on every page, for every developer, from spike S3 until M13. The router basepath stays a runtime value and `router.basepath` stays `/`, or `.` would be baked into the router and the server-function base | OPS-10, R3 |
| D40 | **An explicit `sslmode` in the connection string beats the not-localhost heuristic.** `database.ssl` still wins when set; unset, the URL is consulted before the fallback. The fallback's assumption — not localhost means the wire is untrusted — is right for a hosted database and wrong for the reference deployment, where the host is `postgres` on a private compose network: every operator following the quick start would have met `Client network socket disconnected before secure TLS connection was established` against a URL that already said `sslmode=disable`. The TST-8 smoke test met it first. `prefer` and `allow` are deliberately **not** honoured — they mean "try, then fall back", which this deployment cannot express, and silently downgrading because a URL said `prefer` is the wrong direction for a security default; both fall through to the heuristic. `verify-ca` maps to `verify-full` | CFG-4, OPS-7 |
| D41 | **`azp` on a key-issued JWT comes from a marker, not from the token type.** The api-key gate stamps a `Symbol.for` key on the session it lets through, and the claims builder reads that to choose between `apiKeys.tokenClientId` and the IdP's own id (FR-KEY-3). A symbol because `JSON.stringify` ignores symbol keys — `/get-session` answers exactly what it answered before — and because no request body, database row or JSON payload can produce one, so a caller cannot claim to be an API key by sending a field; `Symbol.for` rather than `Symbol()` because the server bundle and the CLI are separate module graphs and a marker matching in only one of them fails silently in the other. Deriving it from the session's token type instead made every session JWT claim `azp: "idp"` and turned `apiKeys.tokenClientId` into configuration that did nothing | FR-KEY-3, FR-OIDC-7 |
| D42 | ~~**`idp reset-admin` never promotes, and never creates an address typed on the command line.** It resets the account named by `admin.bootstrap.email` — password back to `admin.bootstrap.password`, `mustChangePassword` back to true, status active/unbanned/verified, every session deleted — under the bootstrap advisory lock. If the address exists and holds no admin role the command refuses, or a local command with database access would be a one-line privilege escalation for any account in the deployment. Creating is the degenerate case and only for the configured address: with create-if-missing, one typo in `idp reset-admin adnim@example.com` quietly provisions a second administrator instead of failing. It replaces the `idp create-admin` named in OPS-6 and FR-ADMIN-1, which was never implemented under that name; before it existed the only documented recovery from a lost bootstrap password was `drop schema idp cascade`~~ — **superseded by D52**: the command is removed with the bootstrap account it recovered | OPS-6, FR-ADMIN-1 |
| D43 | **`docker-compose.yml` pins the four variables that decide *which* database and *which* configuration.** `environment` wins over `env_file`, and `IDP_CONFIG_DIR`, `DATABASE_URL`, `DIRECT_DATABASE_URL` and `IDP_SCHEMA_NAME` are each pinned there against a developer's `.env` leaking a host value into the container: a host `IDP_CONFIG_DIR=./config` makes the container die on every restart looking for a directory that is not in it, and a host `DIRECT_DATABASE_URL` would make the container *serve* from the compose Postgres while *migrating* somebody else's — which happened on the first run of the TST-8 smoke test, and was refused only because the SSL mode did not match. The remaining bootstrap variables (`HOST`, `PORT`, `LOG_LEVEL`, `IDP_MIGRATE_ON_BOOT`) need no pin: each applies only where the config file leaves the setting out, and the shipped `config.example` sets all of them. **Amended by D48**: the two database URLs now arrive through `env_file` instead, which a host shell value cannot override either, so the protection stands while the pin list shrinks to `IDP_CONFIG_DIR` and `IDP_SCHEMA_NAME` | OPS-7, CFG-3 |
| D44 | **`site.logo` accepts both `logo.svg` and `branding/logo.svg`.** The schema describes the value as a path *under* `branding/`; the shipped `config.example/config.json` has always shown it relative to the config folder. Whichever an operator copied, the file they mean is `${configDir}/branding/logo.svg`, so a leading `branding/` is dropped rather than doubled. The alternative was serving `/branding/branding/logo.svg` to half of them and 404ing the other half | CFG-4, CFG-1 |
| D45 | **Discovery advertises `{baseUrl}/.well-known/jwks.json` as `jwks_uri`.** FR-OIDC-15 originally named `{baseUrl}/api/auth/jwks` as the canonical one and the well-known path as the copy; M8c inverted that when it moved the protocol endpoints to the issuer root, and the amendment never followed. Inverted is right, and the reason is FR-OIDC-16's own concession: a deployment may keep the UI internal and expose only what a verifier needs. With `jwks_uri` under `/api/auth`, that deployment advertises a key set Neon cannot fetch — so the one prefix an operator has to publish is `/.well-known/`, and the advertised URL has to live inside it. Both paths still answer with the same body, so nothing that already found the other one breaks | FR-OIDC-15, FR-OIDC-16, OPS-9, DOC-2 |
| D46 | **`form-action` allows the registered redirect origins, not only `'self'`.** SEC-4 was written as `form-action 'self'`, which is the right instinct — it is what stops an injected script moving the sign-in form's action — and it also made **every OAuth login impossible in Chrome**. The sign-in form posts to this origin and the answer is a 303 to the client's redirect URI; Chromium applies `form-action` to the redirect a submission *follows*, not only to where it is posted, so the navigation was cancelled with `ERR_ABORTED`. The browser sat on a filled-in sign-in form while the server had already issued the authorization code, and the only trace was a console refusal. Firefox does not check redirects, which is how it survived every manual walk-through. The allowance is exactly FR-OIDC-17's CORS list — the origins the operator has already trusted with authorization codes — so the policy is never wider than the deployment's own client file, and `'self'` still governs everything else. Found by the TST-6 suite, which is the only layer that drives a browser | SEC-4, FR-OIDC-17, FR-OIDC-9 |
| D47 | **RP-initiated logout asks with `/sign-out`, and the provider's own confirmation never reaches a browser.** 1.7.1 renders an unbranded `<h1>Confirm logout</h1>` page whenever the hint does not resolve to the current session, and completes through `/oauth2/end-session/confirm` using a signed, short-lived cookie that page sets. This deployment now forwards the GET, and when the answer is HTML redirects to `/sign-out` **carrying that `Set-Cookie`**; `/sign-out` posts `action: "confirm"` to the provider's confirm endpoint. Two defects were fixed on the way, neither of which anything had ever exercised — FR-OIDC-11 had no behavioural test before M13: the route used to skip the provider entirely when no hint was present, so no confirmation cookie was ever minted and the POST that followed could only fail; and `/sign-out` posted the original request back to `/oauth2/end-session`, which simply asked the same question again. **Whether the provider asks at all** depends on it verifying `id_token_hint` against the key set it fetches from `server.baseUrl` — an ordinary deployment resolves its own issuer and goes straight through, which is why OPS-9 now says the issuer must resolve from inside the deployment | FR-OIDC-11, OPS-9, FR-ACCT-2 |

| D48 | **The environment contract is whole connection strings.** `DATABASE_URL` and `DATABASE_URL_ADMIN` (renamed from `DIRECT_DATABASE_URL`, clean break, no alias — the config key `database.directUrl` is unchanged), passed to the container through `env_file` rather than assembled by compose. The old arrangement made the operator write one password in three places — `POSTGRES_PASSWORD` in `.env`, the same value again in `secrets/postgres_password`, and a `DATABASE_URL` compose interpolated from the first — and required all three even for a deployment pointing at Neon, which uses none of them. `POSTGRES_PASSWORD` becomes an optional, defaulted knob of the bundled `expose:`-only reference database, and the docker `secrets:` block is dropped: a file whose only reader is a database nobody exposes bought a narrow gain at the cost of a mandatory step in every quick start. `env_file` is immune to host-shell overrides, so D43's leak protection survives the move | OPS-7, CFG-3, CFG-4 |
| D49 | **The display name is derived, never typed.** `site.nameFormat` (`first-last` default, `last-first`) composes `user.name` from the captured first and last name, everywhere one is written: sign-up, first-run setup, admin create, admin edit, the account page (where it is shown read-only) and the social profile mapping when the provider ships `given_name`/`family_name`. Two people typing "Jane Smith" and "Smith, Jane" into a free-text field produce a user list that cannot be sorted and a `name` claim that means two different things; the parts are stored as parts, so changing the setting and re-saving re-derives without losing anything. A provider with only `name` keeps it — splitting it would be guessing at a surname. No backfill: existing rows re-derive on their next edit | FR-SIGNUP-5, FR-ACCT-1, FR-ADMIN-2, CFG-4 |
| D50 | **Admin-registered OAuth clients coexist with file-managed ones.** The marker is `oauth_client.user_id`: `NULL` for a file client, the creating administrator for one registered at `/admin/clients` — and reconciliation's orphan sweep was *already* scoped to `user_id IS NULL`, so the rows survive every restart untouched. File rows stay immutable in the UI, because an edit there is one the next restart would undo. The provider needs no restart: its `getClient` falls back to a database lookup for ids outside `cachedTrustedClients`, so a new client works on the next request; `cachedTrustedClients` and `clientPrivileges: () => false` are unchanged, and Better Auth's own client CRUD stays dead. The one real gap was `clientOrigins()`, which fed CORS *and* the CSP `form-action` list from the configuration file alone — a database client's login would have failed in Chrome with nothing in the log naming an origin — so the set becomes file ∪ **enabled** database clients, cached process-locally (correct under OPS-11) and refreshed at start-up and by every mutation. Endpoints are admin-gated and fresh-session gated, validate with the `oauth_clients.json` entry schema, generate the secret server-side and show it once, hash it at rest, run under the reconcile advisory lock, refuse a duplicate id and refuse to touch a file row | FR-OIDC-2, FR-OIDC-4, FR-OIDC-17, FR-ADMIN-2, SEC-4 |
| D51 | **The deployment artefacts live in `docker/`, with pgrest-style lifecycle scripts.** `Dockerfile`, `Dockerfile.dockerignore` (BuildKit's per-Dockerfile ignore), both compose files, both Caddyfiles, and a `.cmd`/`.sh` pair per verb: `idp-create` · `idp-start` · `idp-stop` · `idp-status` · `idp-logs` · `idp-cli` · `idp-destroy`. Nine deployment files at the repository root made the root listing a deployment folder with a source tree in it, and "how do I run this" had no single answer. Everything is invoked from `docker/` with `--env-file ../.env`; the build context stays the repository root. `idp-create` bootstraps `.env` and `config/` from the shipped examples so a clean checkout needs one command, which is why `.env.example` now ships throwaway secrets for the two example clients — both redirect to `app.example.com`, so they are unusable by construction, and without them a freshly copied `config.example` refuses to load | OPS-7, DOC-1 |
| D52 | **The environment bootstrap and `idp reset-admin` are removed; a first-run setup page replaces both.** While the `user` table is empty the IdP asks for the initial user — e-mail, first name, last name, password — creates them as an administrator under the `bootstrapAdmin` advisory lock with an empty-table re-check, signs them in, and closes the page for good. No credential in `.env`, no forced-change dance, no "unset the variables after first boot" footgun — which is the recorded root cause of this deployment's credentials incident, and the reason `reset-admin` had to exist at all: the bootstrap account stranded itself the moment its password was forgotten. The gate is "no users", not "no admin", so it can never mint a second administrator on a running deployment. The accepted cost is that lockout recovery now needs database access: a second administrator, the password-reset e-mail, or one documented SQL statement (`docs/runbooks.md`) | FR-ADMIN-1, OPS-2, OPS-6, CFG-4 |
| D53 | **The default password minimum is 10, not 12.** `auth.password.minLength` has been configurable since M2 (bounds 8–128) and the deployment's own operator was the one who found 12 tiresome; the default is what almost everyone runs. Length remains the only strength rule — composition rules push people towards `P@ssw0rd1` — and the floor of 8 in the schema is unchanged, so a deployment can go lower but not below what NIST calls a minimum. The change is the default plus the two places that ignored the configured value: the sign-up/reset hint hard-coded `passwordHint(12)`, and the admin "set a temporary password" input had neither `required` nor `minLength`; the server had always enforced the configured value through Better Auth's `minPasswordLength` | FR-AUTH-1, §6 |
| D54 | **The first-run wizard requires both names and asks for the password twice.** The names were optional inputs on a form whose only other field is mandatory, and the display name is derived from them (**D49**) — so skipping them left the deployment's first administrator rendering as a stray space in the user list, the audit trail and every token's `name` claim, with no way back: `/setup` closes for good once a user exists (**D52**). The repeated password is the same argument from the other side. The account is created verified and is *not* forced to change its password, precisely so there is no second chance to get it right; a typo therefore produces a deployment nobody can sign in to, recoverable only by dropping the schema — which is exactly the class of incident **D52** was written to end. Validation moved out of the route handler into `server/admin/setup-form.ts` so the rules can be asserted without a runtime; the handler keeps the limiter, the lock and the redirects | FR-ADMIN-1 |
| D55 | **The system page lists the discovery URLs, absolutely.** It showed the issuer and left the operator to assemble every well-known URL from it by hand — which is precisely where a sub-path deployment goes wrong, because two metadata URLs are then correct and neither is guessable from the issuer: OpenID Discovery keeps `{issuer}/.well-known/openid-configuration`, while RFC 8414 §3.1 puts the OAuth document at `{origin}/.well-known/oauth-authorization-server{path}`, with the well-known segment *before* the path. `/idp/system` now returns a `discovery` array of `{key, url}` and the page renders it as labelled links. Under a sub-path that is four entries, not three: `Caddyfile.subpath` rewrites **both** origin-root spellings — the RFC 8414 one and the OpenID Discovery one, which is not defined by any spec but which enough clients ask for that the reference deployment serves it — so both are listed and both are labelled as the reverse proxy's, because they sit above this app's mount point and cannot be routes here. An operator running a different proxy has to see the pair to know which rules they are missing; listing one of them would have sent them away with half. `security.txt` is listed only when the file exists, since the route 404s otherwise; the answer is read once at start-up into the admin context rather than by the endpoint, which must not reach for `getRuntime()`. Renaming the key label from "Signing key" to "Active key ID" rides along: the page never showed key material — `/idp/system` selects `id`/`createdAt`/`expiresAt` and nothing reads `jwks.privateKey` — but the label read as though it did | FR-ADMIN-2 |
| D56 | **`pnpm drizzle:reset` is a repository script, not an `idp` command.** Migrations are forward-only (DM-1) and there is no seed step, so the only way back to a clean database is to remove the schema and let the next boot re-create it — and that statement was written out by hand in four places (the integration harness, the S4 spike, the upgrade-rollback runbook, and the instruction in AGENTS.md to drop the schema to get `/setup` back), each time against whatever connection string the shell happened to hold. The script aims it with the configuration the app itself would load: `database.schema` on `database.directUrl`, and nothing else — never `public` (DM-4, Q16), never another schema in the same database. It is kept out of OPS-6 deliberately: the CLI ships inside the container, where a one-word command that destroys the deployment is a hazard with no upside, while the developer who wants a clean database is in the repository. It prints the target first — configuration folder, masked connection string, schema, and how many tables are in it — and then asks about that schema by name, `[y/N]`, defaulting to no: the risk here is not knowing which database the `.env` points at, and the printed block is what answers that, so the prompt stays a yes-or-no question rather than a transcription exercise. `--yes` exists for scripts, and a non-TTY without it refuses rather than assuming. `lock_timeout` is set first, so a still-running dev server produces the sentence that names it instead of an indefinite hang | DM-1, DM-4, OPS-6 |
| D57 | **A rejected `Origin` is its own refusal, not "wrong password".** Better Auth's CSRF middleware turns a post from an untrusted origin away with `INVALID_ORIGIN` before it looks at a credential. `errorCodeFor` did not map it, so it fell through the default arm into `invalid_credentials` and the sign-in page said the e-mail and password combination was not correct — for a request in which no password was ever checked. Reported from the field on 2026-08-26: an operator finished the first-run wizard, was silently bounced to `/login?notice=account_created` because the automatic sign-in hit the same refusal, and then could not sign in with a password they had typed twice minutes earlier. Every deployment meets this the moment the browser is on an address `server.baseUrl` does not name — `127.0.0.1` against the default `http://localhost:3000` is the whole of it — and the server log says `Invalid origin: …` while the page says the password is wrong, so the log and the screen actively disagree. `INVALID_ORIGIN` and `MISSING_OR_NULL_ORIGIN` now map to `untrusted_origin` with a message that names what is actually wrong. SEC-7 is untouched: this discloses nothing, because the caller chose the origin and already knows it, and it is a configuration fact rather than an account fact — the refusal cannot be aimed at somebody else's account to learn anything about it | SEC-3, SEC-7, FR-AUTH-1 |
| D58 | **`pnpm drizzle:reset` says the running app has to be restarted, and counts the connections that prove it.** `lock_timeout` was the guard against resetting under a live server (**D56**) and it does not hold: an idle connection takes no table lock, so the drop succeeds and the server keeps running against a schema that is no longer there. The first-run gate then makes it worse rather than better — it memoises `false` for the life of the process (**D52**), on the stated reasoning that a deployment cannot go back to having no users while it is running, which is exactly the invariant this script breaks — so `/` and `/login` go on insisting the deployment is set up and serve the sign-in page to somebody who has just emptied the database. Reported from the field on 2026-08-26 as "I ran the reset and the create-first-account page does not come back". The script now prints how many other backends are on the database alongside the schema and table count, and its closing instruction says *restart*, not *start*. Counted rather than refused on: this script's own pool can open a second backend and a pooler holds idle ones after the process behind them is gone, so a refusal would fail on a false positive. The memo itself is left alone — the process is broken either way once its schema is dropped, and restarting is the answer to all of it, not only to the wizard | DM-1, OPS-6, FR-ADMIN-1 |
| D59 | **A session that could not be read is not a session that is absent.** `readSession` ended in `.catch(() => null)`, so a refusal and a database failure produced the same answer: an anonymous visitor, redirected to the sign-in page. Found alongside **D58** and reported in the same breath — "an error which did not show in the UI" — because the two compose into the worst version of themselves: the schema is gone, Better Auth logs `Failed query: select … from "idp"."session"`, and the screen shows an ordinary login form, so the operator concludes they have been signed out rather than that the deployment is broken. The same swallow hides every transient outage the same way, on the one code path whose entire job is to say who the caller is. The discriminator is Better Auth's own, not a guess: `dispatch` converts a refusal into an `APIError` and rethrows anything else untouched, so a driver or query failure arrives as a plain `Error`. Those, and any `APIError` that is itself a 5xx, now propagate to the error boundary and render the branded error page; a 4xx `APIError` — expired, revoked, banned — still answers `null`, because that caller does belong on the login page. The cost is accepted deliberately: a database blip that used to look like a sign-out now looks like a failure, which it is | SEC-6, OPS-2, FR-ACCT-1 |
| D60 | **The generated schemas leave `config.example/`, and the examples are `.jsonc`.** Two halves of one complaint from the owner's third walk-through: the shipped folder mixes files an operator edits with files a generator owns, and the files that are documented as JSONC — and are full of comments — are named `.json`, so every editor that opens one lights it up red before a single key has been read. The schemas move to a root `config-schema/`, because `config.example/` is *copied* to make a deployment's `config/` (DOC-1's quick start, `idp-create`), and a generated artefact inside a copied folder is a file nobody owns: the copy goes stale the moment the schema changes and the `--check` gate never sees it. Nothing reads a schema at runtime — the loader strips `$schema` — so the cost is a relative `../config-schema/…` reference that resolves in the repository and in a sibling `config/`, and silently buys nothing anywhere else. The examples are renamed to `.jsonc`, and the loader resolves each logical file `.jsonc` first and then `.json`: an upgrade may not demand that every existing deployment rename three files, so the old spelling keeps working and the e2e, smoke and integration fixtures — which generate plain JSON — keep exercising it. Both spellings of one file present is refused rather than resolved, in the house style: picking one would be a guess, and the deployment silently serving the file the operator was *not* editing is worse than a startup error naming both. `ConfigFileName` stays a union of the logical names and the physical name is substituted once, where issues are formatted (`ResolvedFileNames`); retyping the union through five modules and the two dozen cross-check literals would have cost a wide diff and the exhaustiveness check, and bought nothing an operator can see | CFG-1, CFG-5, TST-2, DOC-4 |
| D61 | **The administration area can be called something other than the deployment.** `site.name` is one string doing four jobs — the sign-in page's branding, the account area's, the TOTP issuer label and the `from` on every e-mail — and the owner's deployment is *Semantius* to the people signing in and *User Manager* to the handful who administer it. `site.adminTitle` is optional and defaults to `site.name`, so nothing changes for a deployment that does not set it, and the fallback is resolved once in `buildUiContext` rather than at each of the eight admin routes. It is deliberately **branding only**: it does not reach `twoFactor.issuer` (an authenticator app entry is renamed by changing it, and every enrolled user's app would keep the old label anyway), Better Auth's `appName`, or the e-mail templates — an e-mail arrives outside the admin area, from the identity provider, and naming it after one screen would be worse than the problem. Two surfaces render it: the shell heading, which covers all eight routes, and the document title, through the first `head()` in the admin subtree, since the deepest match wins and `__root` keeps `site.name` for everything else. CFG-5 has no hot reload, so a change takes a restart. The shipped example sets both keys — the neutral "Example IdP" branding is gone and the tracked change is what makes the key visible in the repository | CFG-4, FR-ADMIN-2, FR-2FA-1 |
| D62 | **A form checks what it can before it posts, and the server still checks everything.** Reported by the owner as three separate findings — a mistyped confirmation costs a round trip, a rejected client registration comes back blank, a re-authentication throws away a filled form — which are one thing: the browser was told nothing and remembered nothing. Two mechanisms, one posture. Client-side: a rule the browser can evaluate is evaluated there and shown inline against its field, at the `PendingForm`'s own `onSubmit`, which already calls its prop first and honours `preventDefault()`. The server check it duplicates is never removed — a form is whatever the caller posts, and validation that lives only in the browser is not validation — so this is the earlier of two gates and not a replacement for one. Requiring JavaScript for it is D31's standing answer. Server-side: a refusal that does reach the server stashes the submitted fields in the house one-shot store and redirects with `?error=…&draft=<handle>`; the landing loader claims the handle, single-use, and the fields come back filled with the dialog reopened. **Passwords are never stashed** — `PasswordField` has no `defaultValue` prop, deliberately, and the helper strips anything password-shaped as a backstop — because the store is a database table and a rejected form is not a reason to put a credential in one. The stash rather than a query string, for the reason `one-shot.ts` gives at length: a URL survives in history, in `Referer` and in every proxy log in between | FR-ACCT-2, FR-ADMIN-2, D31 |
| D63 | **The freshness bounce keeps the form.** FR-AUTH-5's window is measured from `session.createdAt` and is deliberately not refreshed by activity — only signing in again moves it — so fifteen minutes is easy to spend on a twelve-field client registration, and the owner spent them. The bug was never the window: the gate ran *before* the request body was read, so the POST was discarded on its way past and the administrator, having signed in again, landed on an empty dialog. The order is reversed, which is safe because `readSession` reads headers only and `callAuth` builds a fresh `Request` — `login.tsx` has read the body before responding since M8 — and the stale branch stashes the submitted fields and appends `?draft=<handle>` to the `returnTo` it was already carrying. Three things are deliberate. The window is **unchanged**: the AC's twenty-minute session is still prompted, because being prompted is the point and losing the work never was. The draft is stashed for a **stale** session and never for an absent one: `signin_required` is the branch an anonymous caller reaches, these route POSTs sit in front of no limiter, and writing attacker-controlled JSON into `verification` from an unauthenticated request is not a trade worth making. And the TTL is 1800 s rather than the store's 600 s, because this detour is a whole sign-in and may go through a second factor. The notice is its own code, `reauth_draft`, rather than a sentence bolted onto `reauth` — which is also used where nothing was kept, and would then be a lie | FR-AUTH-5, FR-ADMIN-2 |
| D64 | **Creating a user is a dialog on the list, and the default role is ticked.** Two findings that turn out to be one. `/admin/users/new` was a whole route whose only outcome was to come back to `/admin/users` — while the *other* outcome of the same action, the one-time set-password link that FR-ADMIN-2 requires when e-mail is off, already opened as a dialog *there*. One action, two outcomes, two surfaces, and the page in between existed only to be left. Every other admin action has been a dialog since round 2's item 11; this is the convention finally applied to the one that predates it, and the URL is not kept as a redirect because only the users page ever linked to it. The pre-ticked default role is the same argument about honesty: `roles.jsonc`'s `default: true` is what the server falls back to when a create form sends no role at all, so an empty checkbox column never meant "no roles" — it meant "the default, silently", and the administrator found out by looking at the row afterwards. `RoleCheckboxes` already took a `checked` prop for the edit dialog and `fetchRoles()` already returned `isDefault`; nothing about the server's behaviour changed | FR-ADMIN-2, FR-ROLE-1 |
| D65 | **The reset page reads the token before it renders, and says whose account it is.** It knew nothing: it took the token out of the query string, drew a form, and discovered whether the link was any good only after a password had been typed twice — so an expired invitation and a working one were the same screen, and neither said which account it was about. The loader's JSDoc justified this with "a lookup would burn the token", which is true of the POST and was never true of a read: Better Auth's own `GET /reset-password/:token` validator uses the non-consuming `findVerificationValue` against the same `reset-password:<token>` identifier, and the stored value *is* the user id. Doing the same makes this page a validity oracle for a 192-bit token somebody already holds — no capability the framework does not already expose, and nothing disclosed to anyone without one. **"Already used" is not a state**: a spent token's row is deleted, so it is indistinguishable from one that never existed, and the copy says so instead of pretending otherwise (`token_used` was mapped in `auth-errors.ts` and never emitted; it is gone). The **invitation variant** is the same route with `welcome=1`, on the `searchFlag` precedent `/change-password` set with `forced`: it says an administrator created the account — the wording `email.setPassword` has always used and which never reached the page — drops "signing in again will be needed on your other devices", which comes from `revokeSessionsOnPasswordReset` and is meaningless for an account nobody has signed in to, and points an expired or spent link at the administrator rather than at self-service, which may be switched off (FR-MAIL-2 404s `/forgot-password`). Forging the flag changes copy and nothing else. The one-time link dialog on `/admin/users` is labelled with the address too: two creations in a row otherwise leave an administrator with two identical links. The read is a **server function**, not a call from the loader — a loader is isomorphic and that is the exact mistake `check-client-bundle.ts` exists to catch | FR-AUTH-3, FR-ADMIN-2, FR-ACCT-2 |
| D66 | **`guard.ts` owns the `/admin/*` audit trail, and an account made for somebody is `user.created`.** The owner looked for "user created" in the audit filter and did not find it. It was there, as **`signup.created`** with `metadata.by: "admin"` — the same action name as a self-service registration and as the first-run wizard, three different events under one label on a page whose filter lists labels. `signup.created` now means self-service and nothing else. Three real gaps came out of looking: `/admin/create-user`, `/admin/set-user-password` and `/admin/revoke-user-sessions` wrote **no row at all** when called directly, because the only writes were in the route handlers behind the buttons — and FR-ADMIN-6 says the admin API is a supported interface, so a `curl` that creates an account must leave the trail the button does. **`impersonation.started` was written twice** on the UI path, by the guard hook and by `http/admin-actions.ts`. And **`impersonation.stopped` was declared and never written by anything**, a live SEC-6 violation: nothing called `/admin/stop-impersonating`, because an impersonation ended by expiring after an hour or by signing out. Wiring the hook alone would have audited an event that never fires, so the impersonation banner gained a "Stop impersonating" button — a POST, since a GET that ends a session can be fired by an `<img>` tag. Ownership is recorded here to stop the three-way drift that produced all of this: **`guard.ts` for `/admin/*`**, `hooks.ts`'s `auditEventFor` for Better Auth's own endpoints, a manual write only where no hook can see the event (the setup wizard, which is neither). Two metadata degradations are accepted and named rather than papered over: a created user's target id comes from `ctx.context.returned.user.id`, because the account did not exist when the request was made, and `set-user-password`'s `temporary: true` is dropped, because it was a property of the *route* — which follows the password with a second call setting `mustChangePassword` — and half-deriving it would make the flag mean "probably" | SEC-6, FR-ADMIN-5, FR-ADMIN-6 |
| D67 | **An admin endpoint does the same thing however it is called, and "sign them out everywhere" includes the tokens.** `docs/admin-api.md` documents `/admin/revoke-user-sessions` as signing a user out everywhere and FR-ADMIN-6 makes the admin API a supported interface, and it was not true of the API: Better Auth's admin plugin deletes `session` rows and knows nothing about the tokens this deployment issues, so the OAuth half — `revokeAllForUser` — had to be somebody's, and it had been written into the *route handler behind the button*. A `curl`, a script or an admin API key never goes through a route handler, so a direct call ended the browser session and left the refresh token minting access tokens for as long as the client cared to ask, which is precisely what FR-OIDC-12 forbids. Found while doing **D66** and recorded as a follow-up rather than fixed, which was the wrong call: the endpoint's documented promise was false in the meantime, and the fix is fifteen lines. It moves to `buildAdminAfterHook`, which is a Better Auth `after` hook and therefore runs for every caller — the same consolidation D66 made for the audit row, and for the same reason. The scope is deliberately unchanged: `endsAccess` already covered ban, delete and reject on both paths, and this adds the one action that names revocation in its own title. Nothing else gains a revocation — creating an account and setting a password are not "sign them out everywhere", and doing it there would be a surprise. The integration test posts to the endpoint directly, with no route handler anywhere in the call, and asserts the refresh tokens are gone; reverted against the old code it fails with two live tokens, which is the bug it was written from | FR-ADMIN-6, FR-OIDC-12, SEC-6 |
| D68 | **The CSRF origin check follows the request when nothing is configured.** `server.trustedOrigins` defaulted to `[server.baseUrl]`, which makes signing in require that the deployment *know its own public URL at configuration time*. Reported from the field on 2026-08-27 by an operator who does not: the IdP sits behind a reverse proxy whose address it is given later, so every sign-in met **D57**'s refusal permanently, not just the `127.0.0.1`-against-`localhost` case. Considered and rejected: **off by default** — which is what was asked for — because an identity provider whose state-changing endpoints accept any origin is the one deployment where CSRF matters most, and the same operator would have inherited that silently; and **deriving the issuer from the `Host` header**, which SEC-1 forbids for good reason (it moves every URL the IdP emits, including the reset links of the host-header-injection tests). What is left is to compare the browser's `Origin` against the address the request arrived on — `X-Forwarded-Host`, else `Host` — which needs no configuration and is still a check **a cross-site page cannot pass**: it chooses neither side. `Origin` is set by the browser, and neither host header is CORS-safelisted, so a cross-site request that adds one is preflighted and never answered. The header is compared, never stored, never emitted and never used to build a URL, which is why it is read whatever `server.trustProxy` says — that setting governs whose *identity* is believed (`client-ip.ts`), and a forged value here approves only the forger's own origin. The scheme is ignored, because a TLS-terminating proxy that forgets `X-Forwarded-Proto` is exactly the deployment this exists for. Wildcards are refused on the way in (`*` reaching Better Auth's pattern matcher would switch the check off for the request that sent it) and accepted on the way in from configuration, where `*` is the documented way to turn it off. Setting the key pins the check to what it lists, as before. Found while testing it: Better Auth defaults `disableOriginCheck` to **true** under `NODE_ENV=test`, so the whole of SEC-3 was skipped in every test run this repository has ever made — the two refusal cases that now assert it both passed a cross-site sign-in until `advanced.disableOriginCheck: false` was set explicitly | SEC-3, CFG-4, FR-AUTH-1 |
| D69 | **The first administrator holds the default role too.** The wizard stored `adminRoles[0]` and nothing else, so the one account every deployment is guaranteed to have was the one account without the label every *other* creation path assigns — self-registration, the admin create form, and the admin API's own fallback all apply the catalog's `default: true` role. FR-ROLE-1 makes roles downstream labels the IdP evaluates nothing from, which is exactly why this bites where the IdP cannot see it: an application that gates on `user` excluded the person who set the deployment up, and the owner found it by being locked out of their own app. The two names are deduplicated through a `Set`, because a catalog that makes its admin role the default one would otherwise store `"admin,admin"` — a value `splitRoles` tolerates and every screen renders twice. **No migration**: an already-bootstrapped administrator is one checkbox away from the same state on `/admin/users/:id`, and a start-up that rewrites a role column is a start-up that can rewrite the wrong one | FR-ADMIN-1, FR-ROLE-1 |
| D70 | **Behind `/admin/*` a refusal says what it was, and a create that has already succeeded cannot end on an error page.** One field report, two bugs feeding each other. `errorCodeFor` ends in `invalid_credentials` because it was written for the public pages, where SEC-7 requires a wrong password and an unknown address to be indistinguishable — and every admin form was using it, so a valid "Create a user" submission answered *"That e-mail address and password combination is not correct"* in a dialog with no password field in it. The proximate cause is that Better Auth spells the duplicate refusal two ways — `USER_ALREADY_EXISTS` from `/sign-up/email`, `USER_ALREADY_EXISTS_USE_ANOTHER_EMAIL` from `/admin/create-user` — and only the first was mapped; the real cause is the catch-all, which is the same bug class **D57** fixed for the origin check and the reason this gets a mapping rather than one more `case`. `adminErrorCodeFor` pre-maps both spellings to `email_exists`, delegates everything else to `errorCodeFor`, and rewrites its `invalid_credentials` answer to `request_failed` — the ordinary server-error sentence, without the claim about a password. Naming the duplicate is not an SEC-7 regression: the caller is an administrator looking at a searchable list of every account in the deployment, so enumeration is not a capability this discloses. The scope is deliberate and narrow — the admin routes, `http/admin-actions.ts` and `/admin/system`; the public pages, `auth/change-password.ts` (which genuinely checks a password) and the account routes keep the collapse, `/account/security`'s change-email because it *is* an enumeration surface for a non-administrator. The compounding half: the create handler minted the set-password link **after** the account existed, unguarded, so a failure there produced a 500 — and the natural response to an error page is to submit the form again, which is a duplicate, which is what said the password was wrong. The tail is now wrapped, an `ok` answer with no user id is refused instead of minting a link for `""`, and both land on the list with `createdLinkFailed`, which names the two recoveries rather than implying the account was not created. The integration tests pin Better Auth's own identifiers, so a bump that renames one fails a test instead of a dialog | FR-ADMIN-2, SEC-7, D57, D62 |
| D71 | **A success confirmation is a toast, and it consumes the parameter it arrived on.** Every mutation here is a form post followed by a 303, so the confirmation has to survive a navigation: it travels as `?notice=<code>` and the landing loader turns the code into a catalog sentence. That part was right. What was wrong is that **nothing ever removed the parameter**, so the URL went on asserting that the thing had just happened — a reload re-announced the save, Back re-announced it, and a bookmarked `/admin/users?notice=deleted` announced last week's deletion as news. The banner outlived its truth because the URL did, and no amount of styling fixes a message whose lifetime is wrong. So the eight admin and account pages render `NoticeToast`, which shows the sentence once and then strips the parameter. **`history.replaceState`, not `router.navigate({ replace: true })`**: a navigation re-runs the loaders — an RPC round trip to edit a query string — and would re-claim the one-shot handles that `?created=` and `?draft=` siblings on the same URL have already spent; and `history.state` is passed straight through, because TanStack Router keeps its own keys in there and replacing the entry with `null` state detaches the match from the router's history index. **Errors are deliberately not toasted**: a refusal belongs beside the form that caused it, next to the draft it came back with (**D62**), and it should not time out while it is being read. The public auth pages keep their inline notices for the same reason from the other side — on `/logout` and `/forgot-password` the message *is* the page's content, and a page whose only content is a toast is a blank page. The one-time secret dialogs are untouched: a client secret shown once is not a notification. Requiring JavaScript for the confirmation is **D31**'s standing answer, and the component renders nothing on the server so the sentence cannot appear twice. The timeout is 10 s, matching Playwright's default `expect` window, so an e2e assertion is a test of the message and not a race against the dismissal. The component is the shadcn registry's Base UI `toast` used verbatim; **its placement is the registry's** — bottom-right above `sm`, bottom-centre below — rather than the top-right the finding asked for, because the generated file encodes the anchor in its stacking offsets and in eight enter/exit transforms, and re-anchoring it is a fork of a file the next `shadcn add` overwrites | FR-ACCT-2, FR-ADMIN-2, D31, D62 |
| D72 | **An application registered here can be edited, and its secret rotated.** D50 gave `/admin/clients` create, disable and remove, and stopped there — so a typo in a name, a redirect URI that moved, or a scope that should not have been granted meant removing the application and adding it again, which revokes every token and consent it held and hands its operator a new secret they did not ask for. Reported by the owner on 2026-08-27 as "I can only disable or remove". **Better Auth's `/oauth2/update-client` stays unreachable**, for exactly the reasons D50 gave for its registration endpoint: it is scoped to the client's creator rather than to an administrator, it does not know half of these fields, and it validates against nothing this deployment recognises — no `clientSchema`, no `oauth.scopes` bound, no reconcile lock, no audit row. Two house endpoints instead, beside the three that exist. **The update is a full replace**, because the dialog's field set *is* the writable surface and a patch would need a way to distinguish "unset this" from "did not mention it". Four columns are not the body's to write and each is a way to break a working client silently: **`user_id`** — `toClientRow` defaults it to `null`, which is the file marker, so an update that forgot it would hand the row to the next reconcile's orphan sweep and the application would keep working right up until the restart; **`disabled`**, whose schema default is `false`, so correcting a suspended client's name would switch it back on; **`created_at`**; and the **client id**, which is the natural key `token`, `oauth_consent`, `oauth_client_resource` and the audit trail all reference — changing it is removing this application and adding a different one, which is what Remove is for. The secret follows the type: confidential → confidential passes the **stored hash** through untouched, so the secret already deployed goes on working; public → confidential mints one and shows it once, through the same one-shot stash a creation uses; confidential → public discards it. The whole merged entry is re-validated rather than only the changed fields, because a type change re-checks the stored URIs — a private-use scheme is legal for `native` and refused for `web`, and this is the only moment that can be caught. **Revocation is scoped to that flip and to nothing else**: public and confidential are different ways of authenticating, so a token issued under one is not evidence under the other — while a rename or a moved redirect URI revokes nothing, matching what reconciliation already does for an edited file entry, because the alternative is that fixing a typo signs everybody out. **Rotation is deliberately smaller.** It writes one column, takes no advisory lock (`set-client-disabled`'s precedent: a single-row single-column write races nothing the sweep does), refreshes no origins, and **revokes nothing** — it is hygiene, not incident response; the client is still the client, and an administrator who believes a secret is compromised has Disable and Remove, both of which do revoke. There is no dual-secret grace window in v1, so the old secret stops working immediately and the confirmation dialog says so before it is confirmed. A public client is refused with `CLIENT_HAS_NO_SECRET` rather than quietly given one: that is an edit, and the update endpoint is where an edit belongs. `AdminClientRow` gained `enableEndSession` in the same change — it has no column on the table, so without it the edit dialog could not prefill it and every edit would have reset it, which is the failure mode a full replace makes easy | FR-OIDC-2, FR-OIDC-4, FR-ADMIN-2, SEC-6, D50, D64 |
| D73 | **Publishing is its own workflow, on its own trigger, and it had never once run.** OPS-1's tag set — `X.Y.Z`, `X.Y`, `X`, `latest`, `sha-<git>`, amd64 and arm64 — was fully written in `ci.yml`'s `docker` job, behind `startsWith(github.ref, 'refs/tags/v')` on five steps: QEMU, the semver tag patterns, the GHCR login, the multi-architecture push. **A tag push does not match `on: push: branches: [main]`**, so that workflow could not be reached by a tag at all and none of those steps had ever executed, while [docs/release.md](docs/release.md) told the owner that "the tag builds amd64 and arm64, pushes 1.0.0, 1.0, 1, latest and sha-<commit>". Nothing was wrong with the steps; they were guarded on a ref the trigger cannot produce, which is the failure mode a conditional inside the wrong workflow always has — it looks like a feature and reads as green. Adding `tags:` to `ci.yml` was the smaller change and was rejected: every job there is gated on `github.event_name != 'schedule'` and none on the ref, so a tag would have re-run lint, typecheck, unit, integration-with-coverage and the forty-minute two-shape browser suite before it published, against a tree identical to the one that ran them on `main` an hour earlier — and it would have put the publish behind them. `release.yml` runs the **artefact** gates instead, which are the ones that ask about the thing being published: the TST-8 container smoke test, the Trivy scan and the SBOM, all against the amd64 image built first and loaded locally, with the multi-architecture push reusing that build's cache so what reaches the registry is those layers rather than a second build that happens to match. `docs/release.md` keeps requiring the source gates green on the commit being tagged; CI is where they run. **arm64 is emulated, and here that is nearly free** — `docker/Dockerfile` pins `deps` and `build` to `--platform=$BUILDPLATFORM` because their output is JavaScript, so only the runtime stage's COPYs and one `chmod` go through QEMU; native arm64 runners would buy seconds at the cost of a second job and a manifest assembled by hand. Two smaller decisions ride along. **A `workflow_dispatch` rehearses and never publishes**: it builds both architectures to `type=cacheonly` and smokes amd64, so "will this build for arm64?" is answerable before there is a tag to answer it badly, and there is no boolean anyone can get wrong — a dispatch cannot push, a tag always does. **The tag and `package.json` must agree**, checked in a five-minute job before anything is built: the image stamps `IDP_VERSION` from the tag and `/healthz`, `idp version` and `/admin/system` all report it, so a tree that says 0.0.1 while the artefact says 0.1.0 is one whose running version cannot be traced back to a commit. The workspace moved to **0.1.0** with this change. A pre-release (`v0.1.0-rc1`) is published as one — `metadata-action` skips `{{major}}.{{minor}}`, `{{major}}` and `latest` for it, so a release candidate can never become `latest`. **And the arm64 half of OPS-1 did not build.** Running the two-platform build for the first time — which nothing ever had, because the only thing that would have was the publish step that could not be reached — failed at `bun build` with `qemu-x86_64: Could not open '/lib64/ld-linux-x86-64.so.2'`. The `build` stage is pinned to `--platform=$BUILDPLATFORM` and *executes* the Bun binary it copies out of the `bun` stage; the `bun` stage was not pinned, so BuildKit instantiated it once per **target** platform against a `build` stage that exists once in total, and handed that one stage whichever `bun` the other pass had resolved — an architecture mismatch that is invisible to a single-platform build and therefore invisible to every gate this repository had. `FROM --platform=$BUILDPLATFORM oven/bun:…` fixes it; `runtime` stays unpinned, because it is the artefact and the one stage that must be per-architecture. **The operator-facing half is `docker/release.sh`**, adopted from `semantius-app` — the sibling repository that already publishes a multi-architecture image and has cut three releases through the same shape — because a release procedure that is a paragraph in a runbook is one somebody performs differently each time: it checks the preconditions, bumps the three files that carry a version, commits, tags and pushes, and prints what will be published before it asks. Two deliberate divergences from the sibling, both because this workflow is stricter than that one's: a pre-release is **allowed** (that script forbids one because its workflow tags `latest` unconditionally), and a failed `git tag -s` degrades to `-a` rather than aborting after the bump commit has already been pushed, which is the one state that needs a human to unpick. Reading the two together also settled the image's `org.opencontainers.image.source` label, which named `adenin/semantius-idp` while the remote is `semantius/semantius-idp` — the label is how a pulled image says where it came from | OPS-1, SEC-9, TST-8, D51 |
| D74 | **The database is two connection strings, and either one alone is a valid deployment.** `database.url` was **required** and `database.directUrl` optional beside it, which had the requirement backwards: the direct endpoint is the one that must always work â startup, migrations, the CLI and the cleanup job all take session advisory locks, and those do not hold through a transaction-mode pooler (**D27**) â while the pooled endpoint is an optimisation a given Postgres may not even offer. The practical consequence was that an operator holding only the direct endpoint had to file it under the name that describes the pooled one, and one who set only `DATABASE_URL_ADMIN` was **refused outright** for a configuration that works perfectly. Both keys are optional now, with a `superRefine` on the `database` block requiring at least one and naming both env fallbacks when neither is present. Each role falls back to the other, resolved once in `derive.ts` as `databaseUrl` and `databaseDirectUrl` â so a single-endpoint deployment gets the same string for both, which is correct, because a plain Postgres endpoint is already direct. Resolving it in `derive.ts` rather than in the loader is deliberate: the published JSON Schema then says what is true â neither key is required on its own â while every consumer still receives a guaranteed `string`, and there are only six of them. **`runCrossChecks` still warns** when `url` looks pooled and `directUrl` is unset, which is the one combination that is genuinely wrong, and it now guards on `url` being set at all. The docs generator needed a change too: its required column is one boolean per key and had been derived from `MINIMAL`, the smallest file the schema accepts, so it would have gone on marking `database.url` required after it stopped being â it now renders `one of url / directUrl` for the pair, because "either of these two" is not a boolean | CFG-3, CFG-4, OPS-1, D27, D48 |
| D75 | **A workflow that has never run is not a workflow that works, and its failures do not name themselves.** The first CI run and the first release run this repository ever had were minutes apart on 2026-08-27 — `origin/main` had held only the initial scaffold until then — and both died at `Set up job`, the phase that resolves action references and grants the token before any step executes, and whose summary names no step. Three faults, each invisible to every local gate. **One**: `ci.yml`'s trigger is `push: branches: [main]`, so the publish steps guarded on `refs/tags/v*` inside it had never been reachable (**D73**). **Two**: `aquasecurity/trivy-action@0.28.0` is not a version — that repository tags `v0.28.0`. **Three**, and the one worth the entry: `v0.28.0` internally calls `aquasecurity/setup-trivy@v0.2.1`, and **that tag has been deleted** — `setup-trivy` now publishes only `v0.2.6`, `v0.3.0` and `v0.3.1`. A composite action's own dependencies are resolved at the same moment as yours and fail the same way, so verifying the refs you wrote is not enough; the newer `trivy-action` releases pin `setup-trivy` by **commit SHA**, which is the shape that cannot be retagged out from under a build, and `v0.36.0` is used here for that reason. **The diagnosis this entry originally recorded was wrong and is corrected rather than quietly dropped**: with only the first two faults known, `security-events: write` looked responsible, because every failing job requested it and every passing job did not — but the only two jobs that request it are also the only two that run Trivy, so the two explanations fit the evidence identically. The SARIF upload was moved to a workflow artifact on that reasoning and has been moved back, because the confounder resolved the other way. The lesson that generalises is procedural: **a job's failure annotations are readable without admin rights** — `GET /repos/{owner}/{repo}/check-runs/{job_id}/annotations` — while the logs are not, and one call to it named the deleted nested tag exactly where two rounds of correlation had guessed wrong | SEC-9, OPS-1, D73 |
| D76 | **The image was 28 % over its own ceiling, and nothing could see it.** OPS-13 caps the final image at 300 MB "measured in the CI smoke test", and the first CI run this repository ever had - 2026-08-27, because `origin/main` held only the initial scaffold until then - measured **385.8 MiB**. Locally the same check had always passed, reporting 117.9 MiB, and both numbers are honest: the test asks `docker image inspect --format {{.Size}}`, and **Docker Desktop's containerd image store answers with the compressed size** while a runner's classic store answers with the real one. A developer could therefore never fail this gate, which is why OPS-13 already said CI was where it counted. **The cause was `pnpm deploy --legacy --prod`, whose `--prod` does nothing.** pnpm 10 refuses a non-injected workspace deploy without `--legacy` (`ERR_PNPM_DEPLOY_NONINJECTED_WORKSPACE`), and the legacy path copies the whole virtual store: `drizzle-kit`, `prettier`, `vitest`, three `esbuild` binaries, `@rolldown/binding`, `lightningcss` and `caniuse-lite` were all shipping inside the runtime image - 91 MB of compilers and test runners in an artefact whose entire job is to run one bundled server. Four ways out were tried and rejected: `--prod` before the command, `--config.inject-workspace-packages=true` (both still ship them), and `pnpm prune --prod` inside the deployed tree, which deletes it outright. They are removed by name instead, with the step failing loudly if the removal frees nothing - a rename upstream must break the build rather than quietly restore the weight. **Both halves of the owner's answer are applied**: the image is slimmed to about 295 MB on amd64, *and* the ceiling moves to 350 MB, so the target keeps a margin it has never had rather than sitting five megabytes under a number it has never met. TST-8 is what licenses deleting anything from `node_modules`: it starts the image, migrates, completes the setup wizard, signs in and mints a JWT, and a module the prune removed would fail it - all twenty checks pass on the slimmed image | OPS-1, OPS-13, TST-8, D75 |
| D77 | **The e2e mail capture directory was unwritable by the container on Linux, and only on Linux.** Twenty of the suite's tests failed on the first CI run it ever had, every one of them with `no verify-email e-mail ... Captured: nothing`, and every anonymous test passed - the split was exactly "needs a verified user". D30's capture transport writes each message as a JSON file under `IDP_EMAIL_CAPTURE_DIR`, which `docker-compose.e2e.yml` bind-mounts to `/mail` from a directory the Playwright process creates. The image runs as `bun`, **uid 1000** (OPS-1); a GitHub runner runs as **uid 1001**; and a bind mount carries the host's ownership straight through, so the container had nowhere to write. **Docker Desktop cannot reproduce it** - its bind mounts do not enforce host uids - which is why this suite was green on Windows through every milestone and red the first time a Linux runner drove it, and why 78 tests pass locally against the exact image CI failed on. The directory is `chmod 0777` after it is created: it is a `mkdtemp` directory belonging to one run, and the alternative - pinning the container to the host's uid - changes the artefact under test, which defeats the point of driving the published image. `waitForMail`'s message now distinguishes an empty directory from a missing message, because "Captured: nothing" was the entirety of what twenty simultaneous failures said and it pointed at the application rather than at a mount | TST-6, OPS-1, D30 |
| D78 | **A confirmation has to say what it was about, and it has to be able to end.** Four field reports on 2026-08-27, and three of them are the same defect seen from different sides. **One: the toast does not name the account.** "The account has been deleted." is the same sentence for every account, and it lands on `/admin/users`, where the row that could answer *which one* is precisely the thing that has just gone. The address goes on a second line — Base UI's `description`, not the catalog sentence — because folding it into the wording would mean a second string for every notice and a translator deciding where a proper noun belongs in each, while an e-mail address is the same in every language (FR-I18N-1). Most of it needs no plumbing: every action on `/admin/users/:id` comes back to `/admin/users/:id`, and every account page knows its own session, so the loader already has the address. Two redirects do not — a deletion and a creation both land on the list — and there the address travels as a **one-shot handle**, the same stash the client secret and the set-password link use, because `safeUrlForLog` keeps the query string of every path outside `/oauth2/*` and `/api/auth/*`: `?subject=jane@example.com` would write a deleted account's address into the request log of a codebase that anonymises IP addresses for exactly that reason (SEC-5). The dispatcher reads the address **before** the removal, which is a select and not a write, so the rule that everything here goes through Better Auth's endpoints — the rule that keeps the invariants and the audit trail in the path — is untouched. Two notices deliberately go without a subject: `/account/security`'s change-email one is about a *different* address ("Check the new address for a confirmation link" with the old address under it is a contradiction), and `/account/api-keys` and `/account/consents` are about a key and an application rather than an account. **Two: the toast does not always go away.** Base UI pauses every running auto-dismiss timer when the *window* loses focus and resumes it only on the way back, so a confirmation left behind a switched-away window stays on screen for the length of the absence — minutes, or overnight. That is exactly the outliving-its-truth **D71** set out to end, reintroduced by the component that replaced the banner: D71 removed the parameter that made a banner immortal and inherited a timer that can be frozen. Measured rather than reasoned about: against a running instance a toast was still on screen fifteen seconds after a window blur, and gone eleven seconds after the same blur once the fix was in. There is no provider prop for it and the store is not reachable from `useToastManager`, so the fix is a **wall-clock backstop** in `NoticeToast` — a `setTimeout` two seconds past the nominal lifetime that closes the toast by id. Deliberately *not* a replacement for the library's timer: that one still runs and still wins whenever it is running, so hover and keyboard focus still pause the toast, which is the pause WCAG 2.2.1 asks for. The backstop respects the same state, re-arming while `[data-slot="toast"][data-expanded]` is on screen, and it is **not** cleared on unmount, because the toast lives in the root's `Toaster` and outlives the page that raised it. Patching `packages/ui/src/components/toast.tsx` was rejected on AGENTS.md's standing rule: it is registry output and the next `shadcn add` overwrites it. **Three and four: a public client has no secret, and nothing said so.** Registering an application with the form's default type — `spa`, chosen in round 2 because a browser application is what an operator adds here — produced "The application has been registered.", no secret dialog, and a row with no *Rotate secret* control, with nothing anywhere connecting the three facts; reported as two separate bugs, *"the secret does not show"* and *"the option to change the secret is missing"*, which is what a silent correct behaviour looks like from outside. Nothing about the mechanism was wrong: `spa` and `native` are public clients (FR-OIDC-3), they authenticate with PKCE, and `/idp/rotate-client-secret` refuses one rather than quietly minting a secret (**D72**). The three surfaces now say it. The **type field** carries the consequence in both dialogs — only `web` keeps a secret, and changing the type is what issues or destroys one — which is also the answer to the second report, because **an edit that turns a public client into a `web` one mints a secret and shows it once**, a path D72 built and nobody could find. The **notice** distinguishes the two reasons a create or update returns no secret, off the `isPublic` the endpoints already answer with: a public client has none, an update that kept the existing one has nothing to re-show. The **table** says "Public — no client secret" where it said "Public", and `addHelp` promises a secret only for a Web application, which is what it should always have said. A disabled *Rotate secret* button was considered and rejected — a control that can never be enabled is noise, and the row, the field and the notice already answer the question it would have raised | FR-ACCT-2, FR-ADMIN-2, FR-OIDC-3, SEC-5, D71, D72 |
| D79 | **Withdrawn.** This number was assigned to a timezone defect that does not exist: a claim that postgres.js parses `timestamp without time zone` in the process's own zone, so every timestamp read out of the database arrived shifted by the local UTC offset and the freshness gate refused a session seconds old. The driver does parse it that way — but `drizzle-orm/postgres-js` overwrites the client's parsers for oids 1082/1083/1114/1184 with an identity function on `drizzle(sql)` and does its own UTC-correct mapping, so nothing in this application ever saw the naive parse. The probes that appeared to show an hour of drift were raw `postgres()` handles with no `drizzle()` call on them; against the deployment's own database the server clock and the host clock agree to the second. The `types.timestamp` override the row described was therefore inert — drizzle clobbered it immediately after `postgres()` returned — and it has been removed from `db/client.ts`. The symptom that prompted it was real and is answered by **D81**: the session was five hours old, not ten seconds. **The number is not reused**, because a withdrawn decision that leaves a silent gap is one somebody spends an afternoon looking for | — |
| D80 | **Row actions belong in a menu of their own, not stacked under a column that means something else.** `/admin/clients` had four controls per row — Disable, Edit, Rotate secret, Remove — wrapped under the Enabled/Disabled badge in the **Status** column, because that is where **D50** put the first two and **D72** added two more beside them. So a table whose other six columns are one line each had rows four buttons tall, and most of a row's controls sat under a heading that does not describe them. Reported by the owner as "showing actions below Status is ugly", with the fix named: a three-dot menu. The four move into a trailing column whose `<th>` is `sr-only` — a menu button in every row needs no visible heading, but a `<th>` with nothing in it is a column a screen reader cannot name — and Status goes back to being status. The component is the shadcn registry's Base UI `dropdown-menu`, added with the CLI and used verbatim; it added no dependency and touched no `package.json`, because `@base-ui/react` was already there. Three mechanics are load-bearing and each is a way to get a menu-plus-dialog wrong. **The dialogs are controlled and share one piece of state.** A `menuitem` closes its popup as it is activated, so the trigger a `DialogTrigger` needs has ceased to exist by the moment the dialog should appear — `ActionDialog` grew an `open`/`onOpenChange` pair that renders no trigger at all, and one `"edit" | "rotate" | "remove" | null` for the row makes "never two of these at once" true by construction rather than by luck. **A refused edit still reopens itself** (**D62**, **D72**): that was `defaultOpen`, which is meaningless without a trigger, so the claimed draft seeds the row's state instead. **Enable/Disable stays a real form post**, and its `<form>` stays in the row while the menu item is its submitter by `form=` — nesting the form inside the portalled popup would have it unmounted by the menu closing underneath its own submission. `PendingForm` is kept for the double-submit guard; the spinner it would show is moot, because the menu is gone before it could render one. Two smaller decisions. The trigger is named **`Actions for <application>`** rather than "Actions", because there is one per row: an unnamed one is a list of identical controls to a screen reader and an ambiguous locator to a test — the e2e helpers are `openRowMenu`/`openMenuDialog`, and the file-managed row is now asserted to have **no menu** rather than to be missing four buttons, which is the better assertion anyway. And **Remove is not red here, on purpose.** It keeps `variant="destructive"` for its semantics and its position after a separator, but the `menuColor: "default-translucent"` preset stamps `**:data-[variant=destructive]:text-accent-foreground!` on the popup — a translucent, backdrop-blurred surface cannot promise 4.5:1 for coloured text over arbitrary page content, which is presumably why the preset flattens it, and axe cannot measure contrast through a backdrop filter either. Overriding it would be a fork of registry output for a control that only opens a confirmation whose own submit button *is* destructive and whose description says the removal cannot be undone. The axe pass scans the menu **open**, on a client registered and removed for the purpose, because the pages it already covers are scanned before any spec creates a database-managed row | FR-ADMIN-2, FR-OIDC-2, R-1, D50, D72 |
| D81 | **The freshness gate is removed.** FR-AUTH-5 required every sensitive action — password, e-mail, second factor, API keys, revoke-others, and *every admin write* — to sit behind a session younger than `session.freshAgeMinutes`, defaulting to fifteen, or a re-authentication. It is gone, along with the config key, the `reauth` / `reauth_draft` notices and `http/fresh-session.ts`. **It never worked for the accounts that matter here.** A user who signs in through Google, GitHub or Entra — the owner's estimate is 90 % of them — has no password, so the password form the bounce lands on cannot be satisfied at all; their only route is the provider button, which posts no `callbackURL` and therefore drops both the `returnTo` and the draft the bounce had just stashed. And when that route *does* complete it proves nothing: Better Auth's OAuth callback mints a new session and sets the cookie, resetting `createdAt`, while the upstream — with no `prompt=login`, which is a static per-provider option here and not a per-request one — SSOs silently. The gate was opened by a redirect the user did not interact with. So for the majority of accounts it was either impossible to pass or meaningless when passed, and it functioned as designed only for the password minority. **Nobody asked for it**: spec-v0 contains no freshness or re-authentication requirement; FR-AUTH-5's step-up sentence was written here. The pattern itself is sound — GitHub's sudo mode, Entra's Conditional Access sign-in frequency, AWS's `aws:MultiFactorAuthAge`, OIDC's own `max_age` — but all of them are (sensitive action) AND (window lapsed) with windows of hours, scoped to a curated list, and usually opt-in; this was fifteen minutes on every admin write, always on, password-only. Doing it properly means per-request `prompt=login` and freshness read from the id_token's `auth_time`, which is real work and belongs to whoever decides they want step-up. **What replaces it keeps the half that was carrying weight**: `http/require-session.ts` requires a session on every mutating handler and reads it **authoritatively**, past the cookie cache — so a revocation, a ban or a withdrawn approval bites on the next write instead of up to five minutes later. That property was the old gate's, and losing it would have been a real regression. **Better Auth's own `freshAge` is set to `0`**, the documented off switch: left at its default it re-imposes the same rule on `/delete-user` and on `/update-user`'s e-mail change, as an error code rather than a bounce, on exactly the accounts that cannot answer it. D63's draft machinery stays — the error paths on `/admin/clients` and `/admin/users` still stash and restore a rejected form; only the freshness bounce that also used it is gone | FR-AUTH-5, FR-ACCT-1, FR-KEY-1, FR-ADMIN-2, D63, D68 |
| D82 | **`/admin` and `/account` wear a sidebar, and the chrome moved up into the two layout routes.** The owner asked for the shell the sibling `semantius-app` has: a collapsible left sidebar with a user menu in its footer, full-page-width content, a sheet drawer on a narrow viewport. Both areas, on the owner's answer. What was there was a centred column capped at `max-w-6xl`/`max-w-4xl` with a row of nav pills and two ghost buttons in the header, applied **per page** by `AdminShell` (seven pages) and `AccountShell` (five). **The chrome is now in `routes/admin.tsx` and `routes/account.tsx`, not in the shells**, and that placement is the load-bearing part rather than a tidiness preference: `SidebarProvider` holds the open/collapsed state, the mobile sheet's state and a `window` keydown listener, and a layout route's component persists across every navigation inside its subtree — mounted per page, each navigation would remount the provider, snap the sidebar back open and register another listener. The two shells are now a page header apiece (`<h2>` + description + actions), the area's `<h1>` belongs to the layout, and the width caps are gone. One shared `SidebarLayout` **supersedes D66's deliberate duplication of the impersonation banner**: that was right while the two shells were the two ways to reach a signed-in page, and now FR-ADMIN-5 holds by construction. Six consequences worth naming. **`SidebarInset` renders the only `<main>`**, which is also how `/admin/*` finally acquired a main landmark — it had none. **The cross-area link and sign-out moved into the sidebar's user menu**, amending FR-ACCT-1's "the header carries a link": both are still plain anchors, because the other area is a different route subtree and a client-side navigation would pull its whole bundle in to discover whether the visitor is allowed there. **Sign-out is now two steps** — the menu links to `GET /logout`, the branded confirmation page that already existed, which carries the POST. A `<form>` inside a Base UI menu is the D80 problem one layer further in: the popup unmounts as the item is activated, taking the form with it. Nothing in the e2e suite clicked the old header button; `signOut` has always driven `/logout` directly. **The collapse state is a cookie the server reads.** `idp_sidebar_state`, scoped to `ui.basePath` so a sub-path deployment (OPS-10) and a root one on the same host keep their own preference, read in `http/sidebar-cookie.ts` and carried on `AdminGate`/`ProfileView` — not on `UiContext`, which is process-constant and shared by every request. Read after hydration instead, the sidebar would render open and snap shut on every navigation. The registry component writes its own `sidebar_state` at `path=/` unconditionally and nothing reads it; that write is **accepted rather than patched out**, because the file is registry output. **The active entry comes from `useMatchRoute`, not `activeProps`**: the highlight lives on `SidebarMenuButton`'s own `data-active` and the `<Link>` is inside the button, so `activeProps` cannot reach it; fuzzy matching keeps "Users" lit on `/admin/users/$userId`. **The banner offset is a margin, not a `top`.** The impersonation banner is full-bleed above the sidebar row, and the registry's sidebar container is `fixed inset-y-0 h-svh`, so the shell has to push it down by the banner's height — a `--banner-h` custom property, `2.75rem`, which the banner is itself pinned to at `md` and up, where the desktop sidebar is the only thing that exists. `cn()` resolves `h-svh` against `h-[calc(100svh-var(--banner-h))]` — same tailwind-merge group — but **not `inset-y-0` against `top-*`**, which are different groups, so a `top-` offset would sit beside `inset-block: 0` and win only because Tailwind emits longhands after shorthands. That is true today, and measured in the built stylesheet, and it is not something a layout should rest on. A margin cannot conflict with an inset at all. Verified in a browser against the built stylesheet, both themes: banner 44 px, sidebar top 44 to the viewport floor, content column beside it, and the document no taller than the viewport. **The mobile sheet does not close itself.** A client-side navigation does not unmount it, so below `md` a tapped nav entry changed the page *underneath* a drawer that was still covering it and still holding the focus trap — the e2e suite found it on a 390-wide viewport, where the URL was right and the heading behind it was `aria-hidden`. `ShellSidebar` closes it on the **route**, not on each link's `onClick`: an entry added later is then closed by existing rather than by remembering, and the back button, which no `onClick` can see, closes it too. **The brand is the name and nothing else.** The reference shell puts a coloured square beside it; the owner asked for it gone, which is also what both areas showed before the sidebar — `site.adminTitle` / `site.name` as a line of text. The header block is *hidden* on the icon rail rather than reduced to a square, because with no tile there is nothing left to reduce it to: a collapsed brand block would be an empty 8×8 link, unnamed to a screen reader. The mobile sheet sets no `data-collapsible`, so the drawer keeps its title. And **collapsed is an icon rail with tooltips** — the owner's choice, and a divergence from semantius-app, which hides its navigation entirely when collapsed. Deliberate divergences from that shell, all recorded here: a visible `<h1>` in the header row, because three e2e specs read the area's name off one and semantius-app's header is title-less; no theme toggle, the theme being server-resolved configuration; the impersonation banner, which is ours alone. The registry's internal sr-only strings (`Toggle Sidebar` on the rail, the sheet's `Sidebar` title) stay English as a **bounded FR-I18N-1 exception** — they are generated output — while the trigger's own name comes from the catalog as `common.toggleSidebar`, which wins over the component's `sr-only` span. **The identity tile is not the reference's tile.** semantius-app's brand and avatar squares are `bg-sidebar-primary text-sidebar-primary-foreground`, and they carry a white *icon*; ours carry *text* — an initial, or the brand's first letter — and that pairing measures **3.07:1 in the light theme and 2.12:1 in the dark**, under R-1's 4.5:1 and an axe finding on every page in both areas. They use the accent surface instead, **16.04:1** and **14.56:1**, which is what `AvatarFallback` would have used anyway; the measurements sit in `nav-user.tsx` beside the class, because **re-applying a preset resets the tokens** and the next person needs to know to re-measure. That is also why `e2e/layout.spec.ts` locates the trigger by `[data-sidebar="trigger"]`: `SidebarRail` is a second visible button whose registry label matches the catalog string case-insensitively, and an accessible-name locator matches both. The client bundle grew 33 kB, to 712 kB of the 750 kB ceiling | FR-ADMIN-2, FR-ADMIN-5, FR-ACCT-1, FR-I18N-1, R-1, OPS-10, D61, D66, D71, D80 |

### 12.2 Q&A resolutions (owner, 2026-08-23)

| # | Question (abridged) | Resolution |
|---|---|---|
| Q1 | DB as derived mirror vs. no client rows at all | Derived mirror (FR-OIDC-2) |
| Q2 | JWT claim naming | Owner's token doc → standard OIDC names + `roles[]` + static `role` (FR-OIDC-7) |
| Q3 | `roles` array; `role` reserved for static claim | Yes (FR-ROLE-2) |
| Q4 | Drop `stripeCustomerId`; ban OOTB? | Dropped; ban is OOTB → kept (FR-ADMIN-4) |
| Q5 | Profile sync; correct Entra claim vs. e-mail takeover | Sync per provider; identity = `oid`; takeover impossible (FR-SOC-2/4) |
| Q6 | E-mail collision without linking | No linking at all; reject (FR-SOC-2) |
| Q7 | Resend only | Yes (FR-MAIL-1) |
| Q8 | E-mail off + sign-up on | Unverified badge; approval gate stands (FR-MAIL-2) |
| Q9 | `jwt.audience` example default | Required; example = `baseUrl` (CFG-4) |
| Q10 | Session-JWT endpoint; logout; first-party definition | Enabled; `sign-out` clears cookie; first-party = same host (FR-OIDC-14, FR-AUTH-6) |
| Q11 | Impersonation | Optional, default off (FR-ADMIN-5) |
| Q12 | API-key defaults 365 d / 730 d | Accepted (FR-KEY-1) |
| Q13 | Extra access-token claims | `roles[]` yes; `email_verified` no; `picture` no (FR-OIDC-7) |
| Q14 | API-key JWT via `GET /api/auth/token` + `x-api-key` | Accepted (FR-KEY-3) |
| Q15 | Entra tenantId mandatory | Yes; `common` rejected (FR-SOC-5) |
| Q16 | Migrations table inside `idp` schema | Yes (DM-4) |

**Audit defaults — owner sign-off (2026-08-23), all five resolved:**

| Audit default | Resolution |
|---|---|
| (a) `jwt.claims` default `{}` — static `role` only when configured (FR-OIDC-8) | **Confirmed as written.** |
| (b) exact `includeUserData`/`userClaims` semantics (FR-OIDC-7) | **Confirmed as written.** |
| (c) profile-sync conflict = skip update, keep sign-in (FR-SOC-4) | **Overruled → D24**: the sign-in is blocked. |
| (d) startup warning for social-enabled-while-sign-up-off (FR-SIGNUP-4) | **Overruled → D25**: no warning. |
| (e) `client_credentials` tokens carry no `roles` (FR-OIDC-7) | **Moot → D26**: no `client_credentials` in v1. |

### 12.3 Verified facts the spec relies on (2026-08-23)

V1 Better Auth latest = 1.7.1 · V2 no per-entity repository seam, no roles table, no static-client option, token tables FK → `oauth_client.client_id` · V3 access token is a JWT **only** when an audience/resource resolves; reserved claims cannot be overridden; `accessTokenJWT` no longer exists in 1.7 · V4 JWT plugin defaults EdDSA, supports ES256/RS256; keys encrypted with `secret`; rotation + grace options · V5 **Neon validates RS256/ES256 only**, requires `kid`, checks `aud` only when configured, caches JWKS ≤ 1 h · V6 admin plugin has ban/impersonation/CRUD, no approval · V7 account linking on by default, can be fully disabled · V8 1.7 client fields incl. `clientCredentialsScopes` (unused in v1 per D26); first-class resources with seeding and per-client links (inventory re-check = R9) · Entra provider keys accounts on `profile.oid` (1.7.1 source).

---

## 13. Implementation risks (verify in an early spike; each is referenced from its requirement)

| # | Risk | Fallback |
|---|---|---|
| R1 | Injecting the default audience (`jwt.audience`) as the `resource` when the client sends none — via Better Auth `hooks.before` on `/oauth2/authorize` + `/oauth2/token` or an equivalent supported mechanism. | **Pre-authorized (owner, 2026-08-23):** if hooks cannot do it, apply and maintain a `pnpm patch` of Better Auth. No re-escalation needed. (The "require clients to send `resource`" fallback is *not* acceptable — it violates FR-OIDC-5 for naïve clients.) |
| R2 | `enforcePerClientResources` defaults true → reconcile must create `oauth_client_resource` links for the default + per-client audiences. | Set the option false. |
| R3 | Sub-path: TanStack Start `base` / router `basepath` / Better Auth `basePath`+`baseURL` / cookie `Path` must agree; origin-root RFC 8414 route via Caddy. | None — D2 is a hard requirement; spike first. |
| R4 | Reconcile must hash client secrets exactly as Better Auth does (`storeClientSecret: "hashed"`). | Drive create/update through Better Auth's own server-side API instead of direct writes. |
| R5 | Session-JWT endpoint (`definePayload`) must produce the FR-OIDC-7 claim set. | Custom endpoint using the shared claims builder + jwt plugin signing. |
| R6 | Approval/2FA/forced-change/consent continuation order with the pending authorize request held ≤ 10 min; async approval never resumes the flow. | — (design constraint; verify hooks support it). |
| R7 | Profile sync without linking (update on `(providerId, accountId)`; collision skips update only) implementable via Better Auth hooks. | Custom post-sign-in sync step. |
| R8 | `database.schema`: postgres driver `search_path` + drizzle-kit `migrationsSchema` keeps CLI output untouched and everything in `idp`. | Post-process generated schema to `pgSchema()`. |
| ~~R8~~ | **Closed by spike S4 (2026-08-23).** `search_path` is *not* a workable mechanism: Neon's pooled endpoint silently drops the startup parameter. Resolved instead by generating schema-qualified tables from a factory taking `database.schema`, plus a migrator that retargets the schema identifier — see `docs/spikes/s4-schema-placement.md`. The same spike found session advisory locks do **not** hold through the pooler, which is why `database.directUrl` exists (D27). |
| R9 | V8 client-field/option inventory read from `main` — re-verify against the installed 1.7.1 types before freezing the client JSON schema. | Adjust the schema mapping. |
| R10 | Confirm auto-emitted claims (`jti`, `client_id`, `azp`, `sid`), the exact reserved list and the `customAccessTokenClaims` context (session/client available). | Add missing claims via a token-response hook. |
| R11 | JWKS rotation publishes the new key before it signs. | Rotate = create + publish now, sign after a propagation delay ≥ Neon's 1 h cache. |

---

## 14. Traceability (spec-v0 → v1)

| v0 statement (line) | v1 requirements |
|---|---|
| Lightweight IdP as Docker container; pluggable user management; JWT/JWKS for internal apps "like Neon"; lean, secure, self-contained (1) | §1, §3, FR-OIDC-5/15/16, FR-ADMIN-6, SEC-8, OPS-1 |
| Bun, TanStack Start, shadcn/base-ui, Drizzle/Postgres, pnpm, monorepo, Better Auth latest (3) | §4 |
| Manage users & roles; OIDC/OAuth2 endpoints; single tenant, no organizations (5) | FR-ADMIN, FR-ROLE, FR-OIDC-4, §1.3, DM-2 |
| Login UI; social sign-ins (Google/GitHub); password reset; sign-up option; approval option (7) | FR-ACCT-2, FR-SOC, FR-AUTH-3, FR-SIGNUP |
| Admin plugin, api key (9) | FR-ADMIN, FR-KEY (per-user keys only — D26 removed `client_credentials`) |
| User-facing UI with login & reset pages (11) | FR-ACCT-2 |
| Admin UI: manage users and roles, approve users (13) | FR-ADMIN-2, FR-ROLE-2 (assign; definitions via file), FR-SIGNUP-2 |
| oauth_client & role config files; env substitution `${env:…}`; container configured at build (15) | FR-OIDC-2/3, FR-ROLE-1, CFG-1/2 (runtime substitution; baked non-secret config allowed, secrets env-only — CFG-6) |
| Generic config.json (secret, site name, everything) (17) | CFG-4 |
| Standalone or behind Caddy; no fixed path expectations (19) | OPS-9/10 (sub-path required per D2) |
| Common well-known files (21) | FR-OIDC-15 |
| Automated tests (23) | §10 |
| Docker container (25) | OPS-1..13 |
| README with features/benefits/config (27) | §11 |
| Schema: reuse table/field names, only what's needed, ORM via Better Auth CLI (29) | DM-1..3 |

---

## 15. Sign-off checklist (before deriving the implementation plan)

- [x] Owner confirms the five audit defaults (§12.2) — (a) and (b) confirmed, (c) overruled by **D24**, (d) overruled by **D25**, (e) moot under **D26**.
- [x] Owner confirms the R1 fallback policy — a maintained `pnpm patch` of Better Auth is **pre-authorized** (§13, R1).
- [x] Walk-throughs in the plan's verification checklist re-run against **this** document.
- [x] Implementation plan derived; risks R1–R3 and R8 are spiked first (M0).

**Spec signed off 2026-08-23.** Any later change to a numbered requirement is a spec amendment recorded in §12 and `CHANGELOG.md`.
